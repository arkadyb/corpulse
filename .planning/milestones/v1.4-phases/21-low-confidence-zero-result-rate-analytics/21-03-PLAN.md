---
phase: 21-low-confidence-zero-result-rate-analytics
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on: ["21-01", "21-02"]
files_modified:
  - corpulse/backends/base.py
  - corpulse/backends/sqlite.py
  - corpulse/backends/postgres.py
  - corpulse/backends/postgres_async.py
  - corpulse/backends/memory.py
  - corpulse/core.py
  - corpulse/async_core.py
  - corpulse/integrations/qdrant.py
  - corpulse/models.py
  - tests/test_backend_contract.py
  - tests/test_postgres_backend.py
  - tests/test_async_postgres_backend.py
  - tests/test_analytics.py
  - tests/test_async_core_integration.py
  - tests/test_qdrant_wrapper.py
autonomous: true
requirements: [v1.4-02, v1.4-03]
must_haves:
  truths:
    - "Zero-result analytics are grounded in a durable live query signal, not inferred from retrieval rows that only exist when results were returned."
    - "Empty retrieval attempts are recorded once and remain visible through both sync and async surfaces."
    - "Low-confidence analytics continue to use the retrieval aggregate surface unchanged."
    - "The new zero-result signal is exposed through the same read-only, typed, sync/async-parity patterns already established in Phase 21."
  artifacts:
    - path: "corpulse/integrations/qdrant.py"
      provides: "records empty retrieval attempts so zero-result queries are observable in live usage"
    - path: "corpulse/backends/base.py"
      provides: "storage contract for the durable query-attempt aggregate surface"
    - path: "tests/test_qdrant_wrapper.py"
      provides: "live wrapper coverage for empty-query logging behavior"
  key_links:
    - from: "corpulse/integrations/qdrant.py"
      to: "corpulse/core.py"
      via: "empty query attempts flow into the corpulse analytics surface"
    - from: "corpulse/core.py"
      to: "corpulse/backends/base.py"
      via: "zero-result analytics consume the durable query-attempt aggregate"
    - from: "corpulse/async_core.py"
      to: "corpulse/core.py"
      via: "shared pure result-building logic remains the sync/async contract"
---

<objective>
Close the Phase 21 verification gap by introducing a durable live signal for zero-result retrieval attempts and wiring zero-result analytics to that signal without regressing the low-confidence path.

Purpose: make zero-result analytics truthful in real backends and wrapper flows, not only in fixture-based tests.
Output: durable empty-query logging, backend support for the new query-attempt aggregate, updated sync/async zero-result analytics, and live-path tests that prove the signal exists.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-CONTEXT.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-RESEARCH.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-VERIFICATION.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-REVIEW.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-01-SUMMARY.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-02-SUMMARY.md
@corpulse/backends/base.py
@corpulse/backends/sqlite.py
@corpulse/backends/postgres.py
@corpulse/backends/postgres_async.py
@corpulse/backends/memory.py
@corpulse/core.py
@corpulse/async_core.py
@corpulse/integrations/qdrant.py
@corpulse/models.py
@tests/test_backend_contract.py
@tests/test_postgres_backend.py
@tests/test_async_postgres_backend.py
@tests/test_analytics.py
@tests/test_async_core_integration.py
@tests/test_qdrant_wrapper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a durable query-attempt signal to the storage and wrapper layers</name>
  <files>corpulse/backends/base.py, corpulse/backends/sqlite.py, corpulse/backends/postgres.py, corpulse/backends/postgres_async.py, corpulse/backends/memory.py, corpulse/integrations/qdrant.py, tests/test_backend_contract.py, tests/test_postgres_backend.py, tests/test_async_postgres_backend.py, tests/test_qdrant_wrapper.py</files>
  <action>
    Introduce a live query-attempt persistence path that records empty searches as well as successful retrievals, so the zero-result metric can be derived from actual usage instead of synthetic fixtures.
    Keep the low-confidence retrieval aggregate contract intact while extending the storage layer with whatever additional aggregate surface is needed to represent query attempts or empty-result outcomes.
    Update the Qdrant wrappers so query_points()/search() record empty attempts consistently, and lock the behavior with backend and wrapper tests.
  </action>
  <verify>
    <automated>pytest tests/test_backend_contract.py tests/test_postgres_backend.py tests/test_async_postgres_backend.py tests/test_qdrant_wrapper.py</automated>
  </verify>
  <acceptance_criteria>
    <item>Empty retrieval attempts are persisted in a durable way that real backends can aggregate.</item>
    <item>Wrapper behavior records empty queries without affecting the low-confidence retrieval path.</item>
    <item>Backend parity and tenancy behavior remain green across SQLite, Postgres, async Postgres, and in-memory storage.</item>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rebase zero-result analytics on the durable signal and prove live-path parity</name>
  <files>corpulse/core.py, corpulse/async_core.py, corpulse/models.py, tests/test_analytics.py, tests/test_async_core_integration.py</files>
  <action>
    Rework the zero-result summary/detail helpers to consume the durable query-attempt signal from Task 1, while leaving the low-confidence summary/detail helpers untouched.
    Keep the sync and async implementations on the same pure helper builders and preserve the existing `get_*` naming convention for actionable detail methods.
    Add live-path tests that demonstrate the zero-result metric now moves when the wrapper or manual logging path records an empty retrieval attempt.
  </action>
  <verify>
    <automated>pytest tests/test_analytics.py tests/test_async_core_integration.py</automated>
  </verify>
  <acceptance_criteria>
    <item>Zero-result analytics are driven by a real stored signal, not a synthetic fixture-only aggregate.</item>
    <item>Low-confidence analytics remain unchanged and continue to pass parity tests.</item>
    <item>Sync and async zero-result behavior matches on equivalent fixture data.</item>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper / facade → storage analytics | empty-query persistence feeds read-only analytics, so the surface must stay explicit and deterministic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Tampering | zero-result signal | mitigate | Persist empty-query attempts through the same code path on both sync and async wrappers, then aggregate read-only from storage |
| T-21-06 | Information Disclosure | query detail rows | mitigate | Keep detail rows hashed/minimal and avoid reintroducing raw query text into the public API |
</threat_model>

<verification>
Run backend, wrapper, analytics, and async parity tests after each task. Treat any path that still derives zero-result counts solely from retrieval rows as a blocker.
</verification>

<success_criteria>
- Zero-result analytics are backed by a durable empty-query signal in live usage.
- Low-confidence analytics continue to function from retrieval aggregates without regression.
- Sync and async analytics remain symmetric and read-only.
- Tests prove the zero-result signal is observable in live wrapper/backend flows, not only fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/21-low-confidence-zero-result-rate-analytics/21-03-SUMMARY.md`
</output>
