---
phase: 21-low-confidence-zero-result-rate-analytics
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - corpulse/core.py
  - corpulse/async_core.py
  - corpulse/models.py
  - tests/test_analytics.py
  - tests/test_async_core_integration.py
autonomous: true
requirements: [v1.4-01, v1.4-02]
must_haves:
  truths:
    - "Corpulse exposes low-confidence analytics as both summary and detail methods"
    - "AsyncCorpulse exposes the same semantics with async parity"
    - "Zero-result analytics remain separate from low-confidence analytics"
    - "New payloads stay read-only and align with existing TypedDict conventions"
  artifacts:
    - path: "corpulse/core.py"
      provides: "sync low-confidence and zero-result analytics"
    - path: "corpulse/async_core.py"
      provides: "async parity for the same analytics"
    - path: "tests/test_async_core_integration.py"
      provides: "sync/async parity evidence"
  key_links:
    - from: "corpulse/core.py"
      to: "corpulse/backends/base.py"
      via: "new query aggregate method consumption"
    - from: "corpulse/async_core.py"
      to: "corpulse/core.py"
      via: "shared pure result-building logic"
---

<objective>
Implement the public Phase 21 analytics API for low-confidence and zero-result retrieval signals on top of the new backend query aggregates.

Purpose: give users both quick scalar health metrics and actionable query-level drill-down while preserving sync/async symmetry and keeping zero-result distinct from low-confidence.
Output: new sync and async analytics methods, any needed TypedDict payloads, and parity-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-CONTEXT.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-RESEARCH.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-01-PLAN.md
@corpulse/core.py
@corpulse/async_core.py
@corpulse/models.py
@tests/test_analytics.py
@tests/test_async_core_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sync analytics helpers and public methods</name>
  <files>corpulse/core.py, corpulse/models.py, tests/test_analytics.py</files>
  <action>
    Add the sync Phase 21 public surface using the locked context decisions.
    Implement a summary-plus-detail API for low-confidence analytics and a separate zero-result summary surface, with optional detail if the implementation stays compact.
    Keep the business logic in pure helper builders where possible and preserve the existing `get_*` naming convention for actionable detail methods.
  </action>
  <verify>
    <automated>pytest tests/test_analytics.py</automated>
  </verify>
  <acceptance_criteria>
    <item>Low-confidence analytics expose both a scalar rate and a detail method.</item>
    <item>Zero-result analytics are separate from low-confidence, not aliased into the same metric.</item>
    <item>Typed result payloads remain lightweight and consistent with existing analysis models.</item>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add async parity and cross-surface verification</name>
  <files>corpulse/async_core.py, tests/test_async_core_integration.py</files>
  <action>
    Mirror the new sync analytics in `AsyncCorpulse`, reusing the same result-building semantics after awaiting backend aggregates.
    Add parity tests that prove sync and async corpulse instances return matching outputs from equivalent fixture data.
  </action>
  <verify>
    <automated>pytest tests/test_async_core_integration.py tests/test_analytics.py</automated>
  </verify>
  <acceptance_criteria>
    <item>Async analytics semantics match sync analytics semantics.</item>
    <item>Parity tests cover the new low-confidence and zero-result surfaces.</item>
    <item>No existing report or cleanup-report contract is broken by the new analysis methods.</item>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public analytics API → consumer code | read-only query analytics are surfaced to user code and service layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Information Disclosure | detail methods | mitigate | Return hashed query identifiers and aggregates only; do not attempt to reconstruct raw queries |
| T-21-04 | Repudiation | metric semantics | mitigate | Lock precise low-confidence vs zero-result behavior in tests so later changes cannot silently blur the distinction |
</threat_model>

<verification>
Run analytics and async parity tests together. Treat any sync/async mismatch, contract breakage, or accidental coupling into `report()` / `cleanup_report()` as a blocker.
</verification>

<success_criteria>
- Sync and async APIs expose the locked summary/detail analytics shape.
- Low-confidence and zero-result remain distinct in code and tests.
- New result types fit existing library conventions.
- Test coverage proves parity and guards the semantics of both metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/21-low-confidence-zero-result-rate-analytics/21-02-SUMMARY.md`
</output>
