---
phase: 21-low-confidence-zero-result-rate-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - corpulse/backends/base.py
  - corpulse/backends/sqlite.py
  - corpulse/backends/postgres.py
  - corpulse/backends/postgres_async.py
  - corpulse/backends/memory.py
  - corpulse/models.py
  - tests/test_backend_contract.py
  - tests/test_postgres_backend.py
  - tests/test_async_postgres_backend.py
autonomous: true
requirements: [v1.4-02, v1.4-03]
must_haves:
  truths:
    - "StorageBackend exposes a query-level aggregate surface for Phase 21 analytics"
    - "SQLite, Postgres, async Postgres, and in-memory backends return the same aggregate shape"
    - "The new aggregate shape is sufficient to derive both low-confidence and zero-result metrics without schema changes"
  artifacts:
    - path: "corpulse/backends/base.py"
      provides: "extended backend contract for query aggregates"
    - path: "corpulse/models.py"
      provides: "typed query aggregate rows"
    - path: "tests/test_backend_contract.py"
      provides: "contract lock for new aggregate method"
  key_links:
    - from: "corpulse/backends/base.py"
      to: "corpulse/backends/sqlite.py"
      via: "concrete implementation of new query aggregate method"
    - from: "corpulse/backends/base.py"
      to: "tests/test_backend_contract.py"
      via: "frozen contract assertions"
---

<objective>
Extend the storage layer with query-level aggregate support needed by low-confidence and zero-result analytics.

Purpose: make query-centric analytics a backend capability so sync and async facades can remain thin and consistent.
Output: updated storage contract, concrete backend implementations, typed aggregate rows, and backend/contract tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-CONTEXT.md
@.planning/phases/21-low-confidence-zero-result-rate-analytics/21-RESEARCH.md
@corpulse/backends/base.py
@corpulse/backends/sqlite.py
@corpulse/backends/postgres.py
@corpulse/backends/postgres_async.py
@corpulse/backends/memory.py
@corpulse/models.py
@tests/test_backend_contract.py
@tests/test_postgres_backend.py
@tests/test_async_postgres_backend.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the query aggregate contract and typed row shape</name>
  <files>corpulse/backends/base.py, corpulse/models.py, tests/test_backend_contract.py</files>
  <action>
    Add a typed query-level aggregate row model and extend `StorageBackend` with a new query aggregate method for Phase 21.
    The row shape must cover grouped `query_hash` analytics, including enough data to separate low-confidence queries from zero-result queries without requiring raw query text or schema changes.
    Update the frozen backend contract tests and any fake backends used by contract/parity tests to reflect the new method.
  </action>
  <verify>
    <automated>pytest tests/test_backend_contract.py</automated>
  </verify>
  <acceptance_criteria>
    <item>The backend contract explicitly includes the new query aggregate method.</item>
    <item>The typed row shape is stable and shared across sync and async paths.</item>
    <item>Existing backend contract tests remain green after the contract expansion.</item>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement query aggregation across all backends</name>
  <files>corpulse/backends/sqlite.py, corpulse/backends/postgres.py, corpulse/backends/postgres_async.py, corpulse/backends/memory.py</files>
  <action>
    Implement the new query aggregate method in each concrete backend.
    Reuse existing retrieval storage columns (`query_hash`, `rank`, `score`, `retrieved_at`) and keep SQL/aggregation logic backend-local.
    Preserve current tenancy behavior for Postgres schema/table-prefix modes and keep in-memory behavior shape-matched to SQL backends.
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py tests/test_async_postgres_backend.py tests/test_backend_contract.py</automated>
  </verify>
  <acceptance_criteria>
    <item>All backends return the same keys and compatible semantics for query aggregate rows.</item>
    <item>Postgres tenancy modes still rewrite every SQL path correctly.</item>
    <item>No schema migration or ingestion API change is introduced.</item>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| facade → backend aggregate API | Query-level analytics consume grouped retrieval data from each backend implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | backend aggregation | mitigate | Keep aggregation read-only and derived only from persisted retrieval rows |
| T-21-02 | Information Disclosure | query detail rows | mitigate | Expose only hashed query identifiers and aggregate stats, not raw user query text |
</threat_model>

<verification>
Run contract and backend tests after each aggregate implementation change. Treat contract drift, tenancy SQL regressions, or shape mismatches between backends as blockers.
</verification>

<success_criteria>
- Storage contract includes a stable query aggregate method.
- SQLite, Postgres, async Postgres, and in-memory backends all implement it.
- Backend tests and contract tests cover the new aggregate semantics.
- The resulting aggregate data is enough for both low-confidence and zero-result analytics in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-low-confidence-zero-result-rate-analytics/21-01-SUMMARY.md`
</output>
