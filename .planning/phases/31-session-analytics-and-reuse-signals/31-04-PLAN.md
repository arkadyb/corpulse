---
phase: 31
plan: 31-04
name: Documentation and Regression Verification
wave: 4
depends_on: [31-03]
requirements_addressed: [SESS-01, SESS-02]
files_modified:
  - README.md
  - tests/test_session_reports.py
  - tests/test_workload_reports.py
  - tests/test_trace_jsonl.py
  - tests/test_trace_capture.py
  - tests/test_backend_contract.py
  - tests/test_docstrings.py
autonomous: true
---

# Plan 31-04 - Documentation and Regression Verification

## Goal

Document the new session report API and run the regression suite that proves Phase 31 integrates with existing trace capture, JSONL import/export, and workload reports.

## Must Haves

- README explains `session_report()`.
- Documentation names `summary`, `sessions`, and `context_reuse`.
- Documentation says reports use captured/imported traces and do not require LLM-as-judge.
- Regression tests include Phase 30 workload reports and Phase 29 JSONL tests.

## Tasks

<task id="31-04-01" name="Document session reports">
  <read_first>
    <file>README.md</file>
  </read_first>
  <action>
    Add README documentation for `session_report()` near the existing workload and serving report documentation.

    Include a sync example:
    - `session = corp.session_report(window_days=30)`
    - `print(session["summary"])`
    - `print(session["sessions"])`
    - `print(session["context_reuse"])`

    State that `summary` covers request count, turns per session, duration, follow-up rate, and history growth. State that `context_reuse` surfaces repeated refs or content hashes within sessions and does not use semantic matching or LLM-as-judge.
  </action>
  <acceptance_criteria>
    <criterion>`rg "session_report\\(|context_reuse|follow-up rate|history growth" README.md` exits 0.</criterion>
    <criterion>`README.md` states that session reports use captured or JSONL-imported traces.</criterion>
  </acceptance_criteria>
</task>

<task id="31-04-02" name="Run focused regression suite">
  <read_first>
    <file>.planning/phases/31-session-analytics-and-reuse-signals/31-VALIDATION.md</file>
  </read_first>
  <action>
    Run the Phase 31 validation command and fix any failures caused by Phase 31 changes.
  </action>
  <acceptance_criteria>
    <criterion>`pytest tests/test_session_reports.py tests/test_workload_reports.py tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q` passes.</criterion>
    <criterion>Any unrelated pre-existing failures are documented clearly if they cannot be fixed safely.</criterion>
  </acceptance_criteria>
</task>

<task id="31-04-03" name="Confirm requirements coverage">
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/31-session-analytics-and-reuse-signals/31-VALIDATION.md</file>
  </read_first>
  <action>
    Confirm that implementation and tests cover `SESS-01` and `SESS-02` before marking the phase complete.
  </action>
  <acceptance_criteria>
    <criterion>`SESS-01` maps to session summary and history growth tests.</criterion>
    <criterion>`SESS-02` maps to context reuse tests.</criterion>
    <criterion>No replay, cache recommendation, semantic matching, or model-quality behavior was introduced.</criterion>
  </acceptance_criteria>
</task>

## Verification

```bash
pytest tests/test_session_reports.py tests/test_workload_reports.py tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q
```

## Out of Scope

- Replay proof.
- Cache optimization recommendations.
- Generated docs site.
