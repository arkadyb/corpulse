---
phase: 29
plan: 04
title: JSONL documentation and compatibility verification
type: execute
wave: 3
depends_on: [29-02, 29-03]
files_modified:
  - README.md
  - tests/test_docstrings.py
autonomous: true
requirements: [IO-01, IO-02, IO-03]
requirements_addressed: [IO-01, IO-02, IO-03]
must_haves: |
  D-04: Document JSONL schema with complete and privacy-preserving examples.
  D-09: Document privacy-first raw text defaults.
  D-11: Document explicit raw content opt-in.
  D-18: Final verification proves round-trip and compatibility behavior.
---

<objective>
Document the stable workload trace JSONL schema and verify the new import/export API does not regress existing trace capture or backend contracts.
</objective>

<truths>
- D-04: README/API docs must include a small complete example and a privacy-preserving example.
- D-09/D-11: Docs must make raw content export explicitly opt-in.
- D-18: Final verification must prove round-trip preservation and existing compatibility.
</truths>

<threat_model>
T-29-01 Raw data exposure: README must show privacy-first export as the default and state that raw query text/component metadata require explicit opt-in.

T-29-02 Malformed import corruption: README must mention strict default import and permissive mode result counts.

T-29-03 Duplicate import inflation: README must mention duplicate skipping on re-import.
</threat_model>

<tasks>
<task id="29-04-T1" type="execute">
<title>Document workload trace JSONL import export</title>
<read_first>
- README.md
- corpulse/core.py
- corpulse/async_core.py
- corpulse/workload_io.py
- .planning/phases/29-workload-trace-jsonl-import-export/29-CONTEXT.md
</read_first>
<action>
In `README.md`, extend the existing workload trace documentation with a subsection named exactly `### Workload Trace JSONL Import/Export`.

The subsection must include:
- sync export example using `corp.export_rag_request_traces_jsonl("traces.jsonl")`
- sync import example using `corp.import_rag_request_traces_jsonl("traces.jsonl")`
- async export example using `await async_corp.aexport_rag_request_traces_jsonl("traces.jsonl")`
- async import example using `await async_corp.aimport_rag_request_traces_jsonl("traces.jsonl")`
- schema version string `corpulse.rag_request_trace.v1`
- privacy-preserving example that contains `query_hash`, `components`, `timings`, and `captured_at` but not raw `query_text`
- explicit text that raw query text and component metadata are omitted by default and require `include_raw_text=True` / `include_component_metadata=True`
- explicit text that import is append-oriented and skips duplicate trace fingerprints
</action>
<acceptance_criteria>
- `rg "### Workload Trace JSONL Import/Export" README.md` exits 0.
- `rg "export_rag_request_traces_jsonl\\(\"traces\\.jsonl\"\\)|import_rag_request_traces_jsonl\\(\"traces\\.jsonl\"\\)|aexport_rag_request_traces_jsonl\\(\"traces\\.jsonl\"\\)|aimport_rag_request_traces_jsonl\\(\"traces\\.jsonl\"\\)" README.md` exits 0.
- `rg "corpulse\\.rag_request_trace\\.v1|include_raw_text=True|include_component_metadata=True|skips duplicate trace fingerprints|append-oriented" README.md` exits 0.
</acceptance_criteria>
</task>

<task id="29-04-T2" type="execute">
<title>Add docstring coverage for new public methods</title>
<read_first>
- tests/test_docstrings.py
- corpulse/core.py
- corpulse/async_core.py
</read_first>
<action>
In `tests/test_docstrings.py`, add these public method names to the existing sync and async docstring coverage lists:
- `export_rag_request_traces_jsonl`
- `import_rag_request_traces_jsonl`
- `aexport_rag_request_traces_jsonl`
- `aimport_rag_request_traces_jsonl`

If the lists are separated into sync and async sections, put the sync names with `Corpulse` methods and async names with `AsyncCorpulse` methods.
</action>
<acceptance_criteria>
- `rg "export_rag_request_traces_jsonl|import_rag_request_traces_jsonl|aexport_rag_request_traces_jsonl|aimport_rag_request_traces_jsonl" tests/test_docstrings.py` exits 0.
- `pytest tests/test_docstrings.py -q` exits 0.
</acceptance_criteria>
</task>

<task id="29-04-T3" type="execute">
<title>Run final compatibility verification</title>
<read_first>
- tests/test_trace_jsonl.py
- tests/test_trace_capture.py
- tests/test_backend_contract.py
- tests/test_docstrings.py
- .planning/phases/29-workload-trace-jsonl-import-export/29-VALIDATION.md
</read_first>
<action>
Run the final Phase 29 verification command:

```bash
pytest tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q
```

If any failures are caused by Phase 29 changes, fix the implementation or tests before completing execution. Do not change unrelated tests or remove existing assertions.
</action>
<acceptance_criteria>
- `pytest tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
pytest tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q
```
</verification>

<success_criteria>
1. README documents JSONL import/export, schema version, privacy defaults, raw opt-in flags, and duplicate skipping.
2. New public sync and async methods have docstrings covered by tests.
3. Existing trace capture and backend contract tests remain compatible.
4. Phase 29 satisfies `IO-01`, `IO-02`, and `IO-03`.
</success_criteria>

<must_haves>
- Covers `IO-01` through documented stable schema and examples.
- Covers `IO-02` through import docs and final compatibility verification.
- Covers `IO-03` through privacy-first documentation and tests.
- Covers context decisions D-04, D-09, D-11, and D-18.
</must_haves>
