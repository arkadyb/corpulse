---
phase: 07-postgresbackend-sync
plan: 01
type: execute
wave: 1
depends_on:
  - 06-03
files_modified:
  - corpulse/backends/__init__.py
  - corpulse/backends/postgres.py
  - pyproject.toml
  - tests/conftest.py
  - tests/test_backend_contract.py
  - tests/test_core_backend_integration.py
  - tests/test_import.py
  - tests/test_package.py
  - tests/test_postgres_backend.py
autonomous: true
requirements:
  - BACK-04
  - INT-02
must_haves:
  truths:
    - "`PostgresBackend` implements the existing storage contract without changing `Corpulse` analytics code or the SQLite default path."
    - "`import corpulse` and `import corpulse.backends` still work when psycopg is not installed."
    - "A real PostgreSQL database can run the shared parity assertions when `CORPULSE_POSTGRES_TEST_CONNINFO` is provided."
  artifacts:
    - path: "corpulse/backends/postgres.py"
      provides: "Sync PostgreSQL backend with schema auto-init, BYTEA storage, and translated storage errors"
    - path: "corpulse/backends/__init__.py"
      provides: "Lazy export path for `PostgresBackend` that avoids mandatory psycopg imports"
    - path: "tests/test_postgres_backend.py"
      provides: "Driver-level unit tests plus env-gated live PostgreSQL coverage"
  key_links:
    - from: "corpulse/backends/postgres.py"
      to: "corpulse/backends/base.py"
      via: "implements the frozen 8-method storage contract"
      pattern: "class PostgresBackend\\(StorageBackend\\)"
    - from: "corpulse/backends/__init__.py"
      to: "corpulse/backends/postgres.py"
      via: "lazy `__getattr__` export"
      pattern: "PostgresBackend"
    - from: "tests/conftest.py"
      to: "corpulse/backends/postgres.py"
      via: "optional shared backend fixture activation when a live conninfo is available"
      pattern: "CORPULSE_POSTGRES_TEST_CONNINFO"
---

<objective>
Implement the sync PostgreSQL backend and package surface for corpulse without breaking the existing no-psycopg import path.

Purpose: let service code use PostgreSQL for storage while preserving the existing `StorageBackend` seam, SQLite default behavior, and library-style optional dependency rules.
Output: a lazy-loaded `PostgresBackend`, a `[postgres]` extra in `pyproject.toml`, and test coverage split between always-runnable unit checks and env-gated live parity checks.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-postgresbackend-sync/07-RESEARCH.md
@.planning/phases/07-postgresbackend-sync/07-VALIDATION.md
@corpulse/backends/base.py
@corpulse/backends/__init__.py
@corpulse/backends/sqlite.py
@corpulse/backends/memory.py
@corpulse/core.py
@pyproject.toml
@tests/conftest.py
@tests/test_backend_contract.py
@tests/test_core_backend_integration.py
@tests/test_import.py
@tests/test_package.py

<interfaces>
From `corpulse/backends/base.py`:
```python
class StorageBackend(ABC):
    def upsert_document(self, doc_id: str, filename: str,
                        embedding: bytes | None = None,
                        embedded_at: float | None = None) -> None: ...
    def insert_retrieval(self, doc_id: str, query_hash: str,
                         rank: int, score: float, retrieved_at: float) -> None: ...
    def insert_engagement(self, doc_id: str, event_type: str,
                          engaged_at: float) -> None: ...
    def update_source_timestamp(self, doc_id: str, updated_at: float) -> None: ...
    def all_documents(self) -> list[DocumentRow]: ...
    def retrieval_counts(self, since: float) -> list[RetrievalRow]: ...
    def engagement_counts(self, since: float) -> list[EngagementRow]: ...
    def all_embeddings(self) -> list[EmbeddingRow]: ...
    def close(self) -> None: ...
```

Required roadmap surface:
```python
Corpulse(backend=PostgresBackend(conninfo="..."))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the sync Postgres backend and lazy package surface</name>
  <files>corpulse/backends/__init__.py, corpulse/backends/postgres.py, pyproject.toml</files>
  <read_first>
    corpulse/backends/base.py
    corpulse/backends/__init__.py
    corpulse/backends/sqlite.py
    corpulse/core.py
    pyproject.toml
    .planning/phases/07-postgresbackend-sync/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `PostgresBackend` implements the existing `StorageBackend` method names and returns mapping-shaped rows with the same keys as SQLite and InMemory.
    - Test 2: `psycopg` is imported only when `PostgresBackend` is instantiated or explicitly accessed, not when `corpulse` or `corpulse.backends` is imported.
    - Test 3: Schema initialization creates PostgreSQL tables and indexes automatically, embeddings persist as `BYTEA`, and psycopg exceptions raised inside public backend methods become `StorageBackendError`.
  </behavior>
  <action>Create `corpulse/backends/postgres.py` defining `PostgresBackend(StorageBackend)` with constructor signature `__init__(self, conninfo: str)`. Inside the module, add a private helper such as `_load_psycopg()` that imports `psycopg`, `psycopg.rows.dict_row`, and the psycopg error base only when needed; if import fails, raise an `ImportError` telling the caller to install `corpulse[postgres]`. Open a single psycopg connection in the constructor using `psycopg.connect(conninfo, row_factory=dict_row)`, store it on `self._conn`, and call `_init()` immediately. Implement a PostgreSQL schema using exact table names `documents`, `retrievals`, and `engagements`; use `TEXT PRIMARY KEY` for `documents.doc_id`, `BYTEA` for `embedding_vec`, `DOUBLE PRECISION` for timestamps and scores, `BIGSERIAL PRIMARY KEY` for event ids, and `CREATE INDEX IF NOT EXISTS idx_retrievals_doc`, `idx_retrievals_time`, and `idx_engagements_doc`. Implement `upsert_document()` using `INSERT ... ON CONFLICT (doc_id) DO UPDATE` with `COALESCE(EXCLUDED.embedding_vec, documents.embedding_vec)` and `COALESCE(EXCLUDED.embedded_at, documents.embedded_at)` so SQLite's upsert semantics are preserved. Implement `insert_retrieval()`, `insert_engagement()`, `update_source_timestamp()`, `all_documents()`, `retrieval_counts()`, `engagement_counts()`, `all_embeddings()`, and `close()` against the persistent connection; commit after successful writes, rollback on psycopg errors before re-raising `StorageBackendError`, and keep `close()` idempotent. Update `corpulse/backends/__init__.py` so `PostgresBackend` is available via module-level `__getattr__` and cached into `globals()` without eagerly importing psycopg. Update `__all__` to include `PostgresBackend`. In `pyproject.toml`, add `[project.optional-dependencies].postgres = [\"psycopg>=3.2\"]` while leaving existing extras unchanged; do not require `psycopg[binary]` because this repository is a library, not a final application.</action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py tests/test_import.py tests/test_package.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class PostgresBackend\\(StorageBackend\\)" corpulse/backends/postgres.py` finds the concrete backend class.
    - `rg -n "BYTEA|BIGSERIAL|ON CONFLICT \\(doc_id\\)" corpulse/backends/postgres.py` finds PostgreSQL-specific schema and upsert SQL.
    - `rg -n "def __getattr__\\(name\\)" corpulse/backends/__init__.py` finds the lazy export path.
    - `rg -n "postgres = \\[\\\"psycopg>=3\\.2\\\"\\]" pyproject.toml` finds the package extra.
  </acceptance_criteria>
  <done>`PostgresBackend` exists behind a lazy import boundary, stores embeddings as `BYTEA`, initializes its schema automatically, and the package declares a `[postgres]` extra without making psycopg mandatory at import time.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic tests and env-gated live parity coverage for PostgresBackend</name>
  <files>tests/conftest.py, tests/test_backend_contract.py, tests/test_core_backend_integration.py, tests/test_import.py, tests/test_package.py, tests/test_postgres_backend.py</files>
  <read_first>
    tests/conftest.py
    tests/test_backend_contract.py
    tests/test_core_backend_integration.py
    tests/test_import.py
    tests/test_package.py
    corpulse/backends/postgres.py
    corpulse/backends/__init__.py
    .planning/phases/07-postgresbackend-sync/07-RESEARCH.md
    .planning/phases/07-postgresbackend-sync/07-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: The repo has always-runnable tests for lazy import behavior, missing-psycopg failure messaging, translated storage errors, and package metadata.
    - Test 2: Shared backend parity and `Corpulse(backend=...)` integration tests can include a real `PostgresBackend` automatically when `CORPULSE_POSTGRES_TEST_CONNINFO` is present, while skipping cleanly otherwise.
    - Test 3: Live PostgreSQL tests prove the same backend-contract assertions already used for SQLite and InMemory can pass against Postgres without changing analytics code.
  </behavior>
  <action>Create `tests/test_postgres_backend.py` with two groups of tests. First, always-runnable unit tests: monkeypatch `sys.modules` or the backend's import helper so `corpulse.backends.PostgresBackend` can be imported without loading psycopg until instantiation; verify missing psycopg raises an `ImportError` containing `corpulse[postgres]`; verify translated storage errors wrap the psycopg native exception type; and verify schema init and query methods use the expected SQL by substituting a fake psycopg connection/cursor object. Second, live tests marked or guarded by `CORPULSE_POSTGRES_TEST_CONNINFO`: instantiate a real `PostgresBackend`, clear or isolate the test schema/tables for the test run, and verify round-trip behavior for document upsert, retrieval aggregation, engagement aggregation, and `all_embeddings()`. Update `tests/test_import.py` to add a smoke test proving `import corpulse.backends` still succeeds when `psycopg` is absent, and add a lazy export test for `PostgresBackend`. Update `tests/test_package.py` to assert `psycopg>=3.2` appears in `pyproject.toml`. Update `tests/conftest.py` so the shared `backend` fixture includes a `postgres` branch only when `CORPULSE_POSTGRES_TEST_CONNINFO` is set; otherwise keep the existing `sqlite` and `memory` coverage active. Update `tests/test_backend_contract.py` and `tests/test_core_backend_integration.py` only as needed so the shared parity assertions and `Corpulse(backend=PostgresBackend(...))` integration coverage execute automatically for the `postgres` backend id when the env var is present and skip cleanly when it is absent. Do not weaken the existing SQLite and InMemory assertions.</action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py tests/test_import.py tests/test_package.py -q && CORPULSE_POSTGRES_TEST_CONNINFO="${CORPULSE_POSTGRES_TEST_CONNINFO:-}" pytest tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_postgres_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "CORPULSE_POSTGRES_TEST_CONNINFO" tests/conftest.py tests/test_postgres_backend.py` finds the live-test activation path.
    - `rg -n "import corpulse\\.backends|PostgresBackend" tests/test_import.py` finds explicit lazy-import coverage.
    - `rg -n "psycopg>=3\\.2" tests/test_package.py` finds package-extra coverage.
    - `rg -n "backend.*postgres|request\\.param == \\\"postgres\\\"" tests/conftest.py tests/test_backend_contract.py tests/test_core_backend_integration.py` finds shared fixture integration for Postgres.
  </acceptance_criteria>
  <done>The repo has deterministic Postgres unit coverage everywhere and can run the shared parity suite against a real PostgreSQL database whenever `CORPULSE_POSTGRES_TEST_CONNINFO` is provided.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_postgres_backend.py tests/test_import.py tests/test_package.py -q` first. Then, if a real PostgreSQL DSN is available, run `CORPULSE_POSTGRES_TEST_CONNINFO=... pytest tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_postgres_backend.py -q`. Finish with `pytest tests -q`. The phase is only fully verified when the deterministic unit suite is green and the shared parity branch passes with a live PostgreSQL connection string.
</verification>

<success_criteria>
- `PostgresBackend` exists as a lazy-loaded sync backend using psycopg and schema auto-init.
- The package declares a `[postgres]` extra without making psycopg mandatory for base imports.
- Deterministic tests cover lazy import, package metadata, and translated errors everywhere, while live parity coverage activates automatically when a PostgreSQL conninfo is available.
</success_criteria>

<output>
After completion, create `.planning/phases/07-postgresbackend-sync/07-postgresbackend-sync-01-SUMMARY.md`
</output>
