---
phase: 19-typed-async-payload-models
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [corpulse/core.py, corpulse/async_core.py]
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04]

must_haves:
  truths:
    - "IDE autocompletion available for AsyncCorpulse.report() and cleanup_report() results"
    - "cleanup_report() remains analysis-only and does not mutate any document data"
  artifacts:
    - path: "corpulse/async_core.py"
      provides: "Typed return values for the async public API"
  key_links:
    - from: "corpulse/async_core.py"
      to: "corpulse/models.py"
      via: "import"
      pattern: "from .models import"
    - from: "corpulse/core.py"
      to: "corpulse/models.py"
      via: "import"
      pattern: "from .models import"
---

<objective>
Update corpulse/core.py and corpulse/async_core.py to use the new typed models for return values. This provides full IDE support and static safety for consumers while ensuring cleanup_report() remains analysis-only (MODEL-04).

Purpose: Improve developer experience with better autocompletion and type safety.
Output: Updated core.py and async_core.py with model integration and static verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-typed-async-payload-models/19-01-SUMMARY.md
@corpulse/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update internal builder return types in core.py</name>
  <files>corpulse/core.py</files>
  <action>
    Import the models from corpulse.models and update the return type hints for internal builder functions:
    - _build_report_rows: List[ReportRow]
    - _build_report_summary: ReportSummary
    - _build_cleanup_payload: CleanupPayload
    - _build_ghosts, _build_duplicate_pairs, _build_obsolete_documents, etc. with their respective types.
    Also update Corpulse methods like corpus_health() to return the health model.
    Note: Maintain type-only changes; don't change the actual implementation to preserve backward compatibility.
  </action>
  <verify>
    <automated>pytest tests/test_core_backend_integration.py</automated>
  </verify>
  <done>Internal builders are typed correctly in core.py.</done>
</task>

<task type="auto">
  <name>Task 2: Update public async API return types and verify MODEL-04</name>
  <files>corpulse/async_core.py</files>
  <action>
    Import the models from .models and update the return type hints for AsyncCorpulse methods:
    - report(): ReportPayload
    - cleanup_report(): CleanupPayload
    - get_ghosts(), get_duplicates(), etc. with their respective types.
    
    CRITICAL (MODEL-04): Verify that cleanup_report() remains analysis-only. Inspect the method to ensure it only calls internal builders and builders don't call backend mutations like delete_document(). Add a code comment explicitly stating this per requirement.
  </action>
  <verify>
    <automated>pytest tests/test_async_core_integration.py</automated>
  </verify>
  <done>Async methods have correct type hints and cleanup_report() is confirmed non-mutating.</done>
</task>

<task type="auto">
  <name>Task 3: Run static analysis check for return type hints</name>
  <files>verify_types.py</files>
  <action>
    Create and run a temporary script verify_types.py that uses inspect.get_type_hints to confirm that the public methods of AsyncCorpulse return the expected TypedDict models rather than generic dicts.
  </action>
  <verify>
    <automated>python3 verify_types.py</automated>
  </verify>
  <done>Type hints are confirmed present on public API methods via runtime inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public API → Consumer | TypedDict provides structured output; consumer receives dict as before |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Information Disclosure | corpulse/async_core.py | accept | Types are only for IDE support; no security impact |
</threat_model>

<verification>
Ensure that both sync and async report methods return the same structured data as before, but with added type hints. Use inspect.get_type_hints to verify.
</verification>

<success_criteria>
- AsyncCorpulse methods are fully typed.
- Type hints reflect the new models in corpulse/models.py.
- All tests pass (confirming dict backward compatibility).
- cleanup_report() is confirmed to be analysis-only.
</success_criteria>

<output>
After completion, create .planning/phases/19-typed-async-payload-models/19-02-SUMMARY.md
</output>
