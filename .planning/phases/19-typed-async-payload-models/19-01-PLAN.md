---
phase: 19-typed-async-payload-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [corpulse/models.py, corpulse/backends/base.py, corpulse/backends/memory.py, corpulse/backends/sqlite.py, corpulse/backends/postgres.py, corpulse/backends/postgres_async.py]
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03]

must_haves:
  truths:
    - "IDE provides autocompletion for ReportPayload and CleanupPayload keys"
    - "Backend row types are available from a centralized models module"
  artifacts:
    - path: "corpulse/models.py"
      provides: "Unified TypedDict definitions for all API and backend payloads"
  key_links:
    - from: "corpulse/backends/base.py"
      to: "corpulse/models.py"
      via: "import"
      pattern: "from ..models import"
---

<objective>
Define all TypedDict models in a new corpulse/models.py file and migrate existing backend types. This centralizes the type system and prevents circular dependencies as we add types to the core and async layers.

Purpose: Provide a unified integration surface for IDEs and future FastAPI integration.
Output: New corpulse/models.py and refactored backend type imports.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-typed-async-payload-models/19-RESEARCH.md
@corpulse/backends/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create corpulse/models.py with API and backend models</name>
  <files>corpulse/models.py</files>
  <action>
    Create corpulse/models.py.
    1. Define API models:
       - ReportRow, ReportSummary, ReportPayload (mirroring core.py _build_report_rows/summary)
       - GhostItem, ObsoleteItem, StaleItem, SuspectItem (mirroring core.py _build_* items)
       - CleanupSection (generic or specific for ghosts/obsolete/stale/suspects)
       - CleanupPayload (mirroring core.py _build_cleanup_payload)
    2. Migrate backend models from corpulse/backends/base.py:
       - DocumentRow, RetrievalRow, EngagementRow, EmbeddingRow.
    Use typing.TypedDict for all models to ensure runtime backward compatibility with dict consumers.
  </action>
  <verify>
    <automated>python3 -c "from corpulse.models import ReportPayload, CleanupPayload, DocumentRow; print('Models loaded')"</automated>
  </verify>
  <done>corpulse/models.py exists with all required TypedDict definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor backend type imports</name>
  <files>corpulse/backends/base.py, corpulse/backends/memory.py, corpulse/backends/sqlite.py, corpulse/backends/postgres.py, corpulse/backends/postgres_async.py</files>
  <action>
    1. Update corpulse/backends/base.py: Remove TypedDict definitions and import them from ..models.
    2. Update all backend implementations (memory, sqlite, postgres, postgres_async) to ensure they correctly use the imported types or import them directly if needed.
    Note: Since base.py is the main entry point for these types in backends, updating its exports should cover most cases, but verify each file.
  </action>
  <verify>
    <automated>pytest tests/test_backend_contract.py tests/test_postgres_backend.py tests/test_async_postgres_backend.py</automated>
  </verify>
  <done>All backends use centralized models without breaking existing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Backend → Models | Internal type flow; no untrusted input crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | corpulse/models.py | accept | Models only define shapes; no data is stored or processed here. |
</threat_model>

<verification>
Verify that all models are exportable and that the backend tests still pass with the new import structure.
</verification>

<success_criteria>
- corpulse/models.py contains all TypedDict definitions.
- Backend row types are moved from base.py to models.py.
- All existing tests pass.
</success_criteria>

<output>
After completion, create .planning/phases/19-typed-async-payload-models/19-01-SUMMARY.md
</output>
