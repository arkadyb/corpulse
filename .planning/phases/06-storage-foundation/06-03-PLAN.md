---
phase: 06-storage-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - corpulse/backends/__init__.py
  - corpulse/backends/memory.py
  - tests/conftest.py
  - tests/test_backend_contract.py
  - tests/test_core_backend_integration.py
autonomous: true
requirements:
  - ABS-04
  - BACK-03
  - BACK-06
must_haves:
  truths:
    - "An in-memory backend can power Corpulse analytics without touching the filesystem."
    - "In-memory semantics match SQLite for the edge cases the analytics layer already depends on."
    - "Backend contract and integration tests pass for both SQLite and in-memory implementations."
  artifacts:
    - path: "corpulse/backends/memory.py"
      provides: "InMemoryBackend with dict/list persistence and SQLite-parity semantics"
    - path: "tests/conftest.py"
      provides: "Shared backend fixture exercising SQLite and in-memory implementations"
    - path: "tests/test_backend_contract.py"
      provides: "Parity assertions covering in-memory semantics and lifecycle"
  key_links:
    - from: "tests/conftest.py"
      to: "corpulse/backends/memory.py"
      via: "backend factory parameterization"
      pattern: "InMemoryBackend"
    - from: "tests/test_core_backend_integration.py"
      to: "Corpulse(backend=InMemoryBackend())"
      via: "explicit backend integration test"
      pattern: "InMemoryBackend\\(\\)"
---

<objective>
Implement the dict-based in-memory backend and prove it matches SQLite-visible behavior for analytics and ingestion.

Purpose: Give Phase 6 a no-file-I/O backend for testability and future backend parity checks.
Output: `InMemoryBackend` plus passing shared contract and core integration coverage for both SQLite and in-memory execution paths.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-storage-foundation/06-RESEARCH.md
@.planning/phases/06-storage-foundation/06-VALIDATION.md
@corpulse/backends/base.py
@corpulse/backends/sqlite.py
@corpulse/core.py
@tests/conftest.py
@tests/test_backend_contract.py
@tests/test_core_backend_integration.py
@tests/test_analytics.py

<interfaces>
From `corpulse/backends/base.py`:
```python
class StorageBackend(ABC):
    def upsert_document(...)
    def insert_retrieval(...)
    def insert_engagement(...)
    def update_source_timestamp(...)
    def all_documents(...) -> list[DocumentRow]
    def retrieval_counts(...) -> list[RetrievalRow]
    def engagement_counts(...) -> list[EngagementRow]
    def all_embeddings(...) -> list[EmbeddingRow]
    def close(...) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement InMemoryBackend with SQLite-parity semantics</name>
  <files>corpulse/backends/__init__.py, corpulse/backends/memory.py</files>
  <read_first>
    corpulse/backends/base.py
    corpulse/backends/sqlite.py
    corpulse/core.py
    .planning/phases/06-storage-foundation/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Retrievals, engagements, and registered documents are persisted fully in memory with no path or file access.
    - Test 2: `upsert_document()` preserves existing `embedding_vec` and `embedded_at` values when the new write passes `None`, matching SQLite `COALESCE`.
    - Test 3: `update_source_timestamp()` is a no-op for unknown docs, and aggregate read methods return the same keys/types SQLite returns, including `filename` on `all_embeddings()` rows.
  </behavior>
  <action>Create `corpulse/backends/memory.py` implementing `InMemoryBackend(StorageBackend)` with in-memory document storage keyed by `doc_id` and append-only retrieval/engagement event lists. Match SQLite-visible semantics exactly: `filename` always updates on upsert; `embedding_vec` and `embedded_at` only update when non-`None` values are passed; `update_source_timestamp()` leaves unknown docs untouched; `retrieval_counts(since)` returns dict rows with `doc_id`, `cnt`, `avg_rank`, and `avg_score` where averages are floats or `None`; `engagement_counts(since)` returns `doc_id` and `cnt`; `all_embeddings()` returns only rows whose `embedding_vec` is not `None`, with each row including `doc_id`, `filename`, and `embedding_vec`. Add idempotent `close()` and export `InMemoryBackend` from `corpulse/backends/__init__.py`.</action>
  <verify>
    <automated>python - <<'PY'
from corpulse.backends.memory import InMemoryBackend

backend = InMemoryBackend()
backend.upsert_document("doc-1", "guide-v1.md", b"vec-1", 10.0)
backend.upsert_document("doc-1", "guide-v2.md", None, None)
backend.insert_retrieval("doc-1", "qhash", 1, 0.75, 20.0)
backend.insert_engagement("doc-1", "opened", 25.0)
backend.update_source_timestamp("missing-doc", 30.0)

embeddings = backend.all_embeddings()
assert embeddings == [
    {"doc_id": "doc-1", "filename": "guide-v2.md", "embedding_vec": b"vec-1"}
], embeddings

retrievals = backend.retrieval_counts(since=0.0)
assert retrievals == [
    {"doc_id": "doc-1", "cnt": 1, "avg_rank": 1.0, "avg_score": 0.75}
], retrievals

engagements = backend.engagement_counts(since=0.0)
assert engagements == [{"doc_id": "doc-1", "cnt": 1}], engagements

documents = backend.all_documents()
assert documents == [
    {
        "doc_id": "doc-1",
        "filename": "guide-v2.md",
        "embedding_vec": b"vec-1",
        "embedded_at": 10.0,
        "source_updated_at": None,
    }
], documents

backend.close()
backend.close()
print("in-memory-backend-parity-ok")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class InMemoryBackend\\(StorageBackend\\)" corpulse/backends/memory.py` finds the concrete in-memory backend.
    - `rg -n "embedding_vec|embedded_at|source_updated_at" corpulse/backends/memory.py` finds the parity state fields.
    - `rg -n "InMemoryBackend" corpulse/backends/__init__.py` confirms the backend is exported.
  </acceptance_criteria>
  <done>An in-memory backend exists with the exact ingestion and aggregate semantics the analytics layer already expects from SQLite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish shared parity and Corpulse integration coverage for the in-memory backend</name>
  <files>tests/conftest.py, tests/test_backend_contract.py, tests/test_core_backend_integration.py</files>
  <read_first>
    tests/conftest.py
    tests/test_backend_contract.py
    tests/test_core_backend_integration.py
    tests/test_analytics.py
    corpulse/backends/memory.py
    corpulse/core.py
    .planning/phases/06-storage-foundation/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: The shared backend fixture now executes the contract suite against both `SQLiteBackend` and `InMemoryBackend`.
    - Test 2: `Corpulse(backend=InMemoryBackend())` records retrievals and analytics without any file I/O setup.
    - Test 3: Lifecycle coverage proves `close()` and context-manager usage work for in-memory and SQLite backends alike.
  </behavior>
  <action>Complete `tests/conftest.py`, `tests/test_backend_contract.py`, and `tests/test_core_backend_integration.py` so both backend ids run through the shared suite. Add explicit assertions that `Corpulse(backend=InMemoryBackend())` can log retrievals, report ghosts/suspects from in-memory state, and operate under `with Corpulse(backend=InMemoryBackend()) as corp:`. Keep SQLite-specific tests isolated in legacy modules; the contract suite should cover parity, not private `_conn()` behavior or WAL assertions.</action>
  <verify>
    <automated>pytest tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_analytics.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "params=\\[\"sqlite\", \"memory\"\\]" tests/conftest.py` shows both backend ids remain active in the shared fixture.
    - `rg -n "Corpulse\\(backend=InMemoryBackend\\(\\)\\)" tests/test_core_backend_integration.py` finds explicit in-memory facade coverage.
    - `rg -n "with Corpulse\\(backend=InMemoryBackend\\(\\)\\)" tests/test_core_backend_integration.py` finds context-manager coverage for the in-memory path.
  </acceptance_criteria>
  <done>Shared backend parity tests and explicit core integration tests prove the in-memory backend works without file I/O and matches SQLite-visible behavior where required.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_analytics.py -q` after the plan, then `pytest tests -q` to confirm the expanded backend coverage still leaves the full suite green.
</verification>

<success_criteria>
- `InMemoryBackend` is implemented with SQLite-parity edge-case semantics.
- Shared backend contract tests run against both SQLite and in-memory backends.
- `Corpulse(backend=InMemoryBackend())` works without file-system setup and supports lifecycle delegation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-storage-foundation/06-storage-foundation-03-SUMMARY.md`
</output>
