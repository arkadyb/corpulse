---
phase: 06-storage-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - corpulse/backends/__init__.py
  - corpulse/backends/sqlite.py
  - corpulse/core.py
  - corpulse/db.py
  - tests/conftest.py
  - tests/test_backend_contract.py
  - tests/test_core_backend_integration.py
  - tests/test_analytics.py
  - tests/test_qdrant_wrapper.py
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - BACK-06
  - INT-01
must_haves:
  truths:
    - "Corpulse still defaults to SQLite with the same behavior and the same regression baseline."
    - "`from corpulse.db import DB` continues to resolve for existing callers and tests."
    - "All backend lifecycle and constructor pathways are explicit instead of silently ignoring conflicting inputs."
  artifacts:
    - path: "corpulse/backends/sqlite.py"
      provides: "SQLiteBackend refactor of the existing DB implementation"
    - path: "corpulse/db.py"
      provides: "Compatibility import surface aliasing SQLiteBackend as DB"
    - path: "corpulse/core.py"
      provides: "Corpulse constructor with explicit backend injection and lifecycle delegation"
  key_links:
    - from: "corpulse/core.py"
      to: "corpulse/backends/sqlite.py"
      via: "default backend instantiation when backend is omitted"
      pattern: "SQLiteBackend\\("
    - from: "corpulse/db.py"
      to: "corpulse/backends/sqlite.py"
      via: "compat alias"
      pattern: "from \\.backends import SQLiteBackend as DB"
    - from: "tests/test_qdrant_wrapper.py"
      to: "corpulse/backends/sqlite.py"
      via: "SQLite-specific `_conn()` helper retained for DB-level assertions"
      pattern: "_conn\\("
---

<objective>
Refactor the existing SQLite persistence layer behind the new backend seam without changing public behavior.

Purpose: Make SQLite the first real `StorageBackend` implementation, preserve the import compatibility surface, and let `Corpulse` accept explicit backends while defaulting to SQLite.
Output: `SQLiteBackend`, a `db.py` compatibility shim, and `Corpulse(backend=...)` wiring that preserves the 41-test baseline.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-storage-foundation/06-RESEARCH.md
@.planning/phases/06-storage-foundation/06-VALIDATION.md
@corpulse/backends/base.py
@corpulse/db.py
@corpulse/core.py
@corpulse/__init__.py
@tests/test_analytics.py
@tests/test_qdrant_wrapper.py
@tests/test_backend_contract.py
@tests/test_core_backend_integration.py

<interfaces>
From `corpulse/core.py` current constructor:
```python
class Corpulse:
    def __init__(
        self,
        db_path: str = "./corpulse.db",
        ghost_threshold_days: int = 30,
        duplicate_threshold: float = 0.92,
        stale_threshold_days: int = 14,
        obsolete_pattern: str = r"v\\d+",
        top_k_report: int = 20,
    ):
        self.db = DB(db_path)
```

Required compatibility surface from research:
```python
from corpulse.db import DB
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the existing DB implementation into SQLiteBackend and activate SQLite contract coverage</name>
  <files>corpulse/backends/__init__.py, corpulse/backends/sqlite.py, corpulse/db.py, tests/conftest.py, tests/test_backend_contract.py</files>
  <read_first>
    corpulse/backends/base.py
    corpulse/db.py
    tests/test_analytics.py
    tests/test_qdrant_wrapper.py
    .planning/phases/06-storage-foundation/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: SQLite remains file-backed, initializes the same schema, and preserves current `_conn()` behavior for SQLite-specific tests.
    - Test 2: Read methods return mapping-shaped rows compatible with analytics and new TypedDict contracts.
    - Test 3: Native `sqlite3` failures raised from public backend methods surface as `StorageBackendError` via exception chaining, and the staged contract suite now runs actively for SQLite.
  </behavior>
  <action>Create `corpulse/backends/sqlite.py` by moving the existing `DB` logic into `SQLiteBackend(StorageBackend)` with the same SQL, the same `_conn()` helper, the same WAL schema string, and the same upsert behavior. Normalize read results to plain dicts matching the TypedDict row contracts instead of returning `sqlite3.Row` objects. Catch `sqlite3.Error` inside public backend methods and re-raise `StorageBackendError` from the original exception. Add `close()` as an idempotent no-op lifecycle method because the backend still opens per-operation connections. Create `corpulse/backends/__init__.py` exporting `StorageBackend`, row types, `StorageBackendError`, and `SQLiteBackend`. Replace `corpulse/db.py` with a compatibility shim that keeps `from corpulse.db import DB` working by aliasing `SQLiteBackend` as `DB` and nothing else. In `tests/conftest.py` and `tests/test_backend_contract.py`, replace the wave-1 skip fence with active SQLite-only fixture coverage while keeping in-memory cases fenced for 06-03.</action>
  <verify>
    <automated>pytest tests/test_backend_contract.py tests/test_analytics.py tests/test_qdrant_wrapper.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class SQLiteBackend\\(StorageBackend\\)" corpulse/backends/sqlite.py` finds the concrete SQLite backend.
    - `rg -n "def _conn\\(" corpulse/backends/sqlite.py` confirms the SQLite-private inspection hook still exists.
    - `rg -n "from \\.backends import SQLiteBackend as DB" corpulse/db.py` confirms the compat shim.
  </acceptance_criteria>
  <done>SQLite now lives behind `StorageBackend` without changing its SQL behavior, import compatibility, or SQLite-specific regression helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Corpulse to accept explicit backends while preserving the SQLite default</name>
  <files>corpulse/core.py, tests/test_core_backend_integration.py, tests/test_analytics.py, tests/test_qdrant_wrapper.py</files>
  <read_first>
    corpulse/core.py
    corpulse/backends/base.py
    corpulse/backends/sqlite.py
    tests/test_core_backend_integration.py
    tests/test_analytics.py
    tests/test_qdrant_wrapper.py
    .planning/phases/06-storage-foundation/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `Corpulse()` still uses SQLite by default and keeps existing analytics and wrapper tests green.
    - Test 2: `Corpulse(backend=SQLiteBackend(...))` works explicitly and `close()` / context-manager support delegate to the backend.
    - Test 3: Passing both `backend=` and a non-default `db_path` raises `ValueError` rather than silently ignoring one input.
  </behavior>
  <action>Update `corpulse/core.py` so `Corpulse.__init__` accepts `backend: StorageBackend | None = None` in addition to `db_path`. If `backend` is `None`, instantiate `SQLiteBackend(db_path)`. If `backend` is provided and `db_path` is not the default `"./corpulse.db"`, raise `ValueError` immediately per the recommended constructor policy. Store the resolved backend on `self.db`, add `close()`, `__enter__`, and `__exit__` methods on `Corpulse` that delegate to the backend, and keep all analytics logic unchanged. In `tests/test_core_backend_integration.py`, replace the staged skip fence with active SQLite-backed `Corpulse(backend=SQLiteBackend(...))`, lifecycle, and conflict-rule assertions. Adjust only the tests that need constructor-usage updates; do not parameterize the legacy SQLite regression suite or remove direct `_conn()` assertions from wrapper tests.</action>
  <verify>
    <automated>pytest tests/test_core_backend_integration.py tests/test_analytics.py tests/test_qdrant_wrapper.py tests/test_import.py tests/test_package.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "backend: StorageBackend \\| None = None" corpulse/core.py` finds the explicit backend constructor parameter.
    - `rg -n "raise ValueError" corpulse/core.py` finds the constructor conflict guard.
    - `rg -n "def close\\(|def __enter__\\(|def __exit__\\(" corpulse/core.py` finds facade lifecycle delegation.
  </acceptance_criteria>
  <done>`Corpulse` accepts explicit backend instances, preserves the SQLite default path, and keeps existing public imports and SQLite-focused regressions intact.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_core_backend_integration.py tests/test_backend_contract.py tests/test_analytics.py tests/test_qdrant_wrapper.py tests/test_import.py tests/test_package.py -q` and then `pytest tests -q`. The full suite should stay at the verified 41-test green baseline while the new backend integration tests are added.
</verification>

<success_criteria>
- SQLite behavior is preserved behind `SQLiteBackend`.
- `corpulse/db.py` remains a compatibility import surface.
- `Corpulse` supports explicit backend injection with the documented `ValueError` conflict rule and lifecycle delegation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-storage-foundation/06-storage-foundation-02-SUMMARY.md`
</output>
