---
phase: 06-storage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - corpulse/backends/base.py
  - tests/test_backend_contract.py
  - tests/test_core_backend_integration.py
autonomous: true
requirements:
  - ABS-01
  - ABS-02
  - ABS-03
must_haves:
  truths:
    - "Backend implementations are held to the frozen 8-method storage contract before refactors begin."
    - "Storage contract test modules exist and pass before any concrete backend implementation work begins."
    - "Storage failures have an explicit translated exception type the rest of the library can target."
  artifacts:
    - path: "corpulse/backends/base.py"
      provides: "StorageBackend ABC, TypedDict row contracts, and StorageBackendError"
    - path: "tests/test_backend_contract.py"
      provides: "Contract-freezing tests plus staged parity placeholders"
    - path: "tests/test_core_backend_integration.py"
      provides: "Default-constructor coverage plus staged backend-injection placeholders"
  key_links:
    - from: "tests/test_backend_contract.py"
      to: "corpulse/backends/base.py"
      via: "imports StorageBackend, TypedDict row shapes, and StorageBackendError"
      pattern: "from corpulse\\.backends\\.base import"
    - from: "tests/test_core_backend_integration.py"
      to: "corpulse/core.py"
      via: "default constructor coverage before backend injection exists"
      pattern: "Corpulse\\("
---

<objective>
Freeze the storage abstraction and create the shared test harness Phase 6 will execute against.

Purpose: Make the existing 8-method DB seam explicit before SQLite refactors and in-memory implementation work begin.
Output: A backend base module plus passing contract/integration scaffolding that freezes the seam now and stages parity coverage for later plans.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-storage-foundation/06-RESEARCH.md
@.planning/phases/06-storage-foundation/06-VALIDATION.md
@corpulse/db.py
@corpulse/core.py
@tests/test_analytics.py
@tests/test_qdrant_wrapper.py

<interfaces>
From `corpulse/db.py`:
```python
class DB:
    def upsert_document(self, doc_id: str, filename: str,
                        embedding: bytes | None = None,
                        embedded_at: float | None = None): ...
    def insert_retrieval(self, doc_id: str, query_hash: str,
                         rank: int, score: float, retrieved_at: float): ...
    def insert_engagement(self, doc_id: str, event_type: str, engaged_at: float): ...
    def update_source_timestamp(self, doc_id: str, updated_at: float): ...
    def all_documents(self): ...
    def retrieval_counts(self, since: float): ...
    def engagement_counts(self, since: float): ...
    def all_embeddings(self): ...
```

From `corpulse/core.py`:
```python
class Corpulse:
    def __init__(
        self,
        db_path: str = "./corpulse.db",
        ghost_threshold_days: int = 30,
        duplicate_threshold: float = 0.92,
        stale_threshold_days: int = 14,
        obsolete_pattern: str = r"v\\d+",
        top_k_report: int = 20,
    ): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the backend contract and translated error surface</name>
  <files>corpulse/backends/base.py</files>
  <read_first>
    corpulse/db.py
    corpulse/core.py
    .planning/phases/06-storage-foundation/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: The contract exposes exactly the frozen 8 persistence methods used by `Corpulse`, plus lifecycle methods `close`, `__enter__`, and `__exit__`.
    - Test 2: Shared row contracts cover documents, retrieval aggregates, engagement aggregates, and embeddings with mapping-style keys that match current analytics usage, including `filename` on embedding rows.
    - Test 3: The abstract contract surface is frozen concretely enough that later plans can detect accidental method drift, signature drift, or missing row/error exports.
  </behavior>
  <action>Create `corpulse/backends/base.py` with `DocumentRow`, `RetrievalRow`, `EngagementRow`, and `EmbeddingRow` `TypedDict`s; a `StorageBackendError(RuntimeError)`; and a `StorageBackend(ABC)` whose abstract data API is exactly `upsert_document`, `insert_retrieval`, `insert_engagement`, `update_source_timestamp`, `all_documents`, `retrieval_counts`, `engagement_counts`, and `all_embeddings`. `EmbeddingRow` must include `doc_id`, `filename`, and `embedding_vec` because live `Corpulse.get_duplicates()` logic reads filename from `all_embeddings()` rows. Add abstract `close()` and shared `__enter__`/`__exit__` implementations. Do not redesign analytics-facing method names or signatures; freeze the current seam from `corpulse/db.py` exactly.</action>
  <verify>
    <automated>python - <<'PY'
import inspect
from corpulse.backends.base import (
    DocumentRow,
    EmbeddingRow,
    EngagementRow,
    RetrievalRow,
    StorageBackend,
    StorageBackendError,
)
required = {
    "upsert_document": ["self", "doc_id", "filename", "embedding", "embedded_at"],
    "insert_retrieval": ["self", "doc_id", "query_hash", "rank", "score", "retrieved_at"],
    "insert_engagement": ["self", "doc_id", "event_type", "engaged_at"],
    "update_source_timestamp": ["self", "doc_id", "updated_at"],
    "all_documents": ["self"],
    "retrieval_counts": ["self", "since"],
    "engagement_counts": ["self", "since"],
    "all_embeddings": ["self"],
    "close": ["self"],
}
assert required.keys() <= StorageBackend.__abstractmethods__, StorageBackend.__abstractmethods__
for name, expected in required.items():
    actual = list(inspect.signature(getattr(StorageBackend, name)).parameters)
    assert actual == expected, (name, actual)
assert "__enter__" in StorageBackend.__dict__
assert "__exit__" in StorageBackend.__dict__
for row_type, keys in {
    DocumentRow: {"doc_id", "filename", "source_updated_at", "embedding_vec", "embedded_at"},
    RetrievalRow: {"doc_id", "cnt", "avg_rank", "avg_score"},
    EngagementRow: {"doc_id", "cnt"},
    EmbeddingRow: {"doc_id", "filename", "embedding_vec"},
}.items():
    assert keys <= set(row_type.__annotations__), (row_type.__name__, row_type.__annotations__)
assert issubclass(StorageBackendError, RuntimeError)
print("backend-base-contract-ok")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class StorageBackend\\(ABC\\)" corpulse/backends/base.py` finds the abstract backend contract.
    - `rg -n "class (DocumentRow|RetrievalRow|EngagementRow|EmbeddingRow)\\(TypedDict\\)" corpulse/backends/base.py` finds all four shared row types.
    - `rg -n "class StorageBackendError\\(RuntimeError\\)" corpulse/backends/base.py` finds the translated backend exception.
  </acceptance_criteria>
  <done>`corpulse/backends/base.py` exists with the frozen contract, shared row types, and explicit translated error type future backends can implement against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add wave-1-safe contract and integration test scaffolding</name>
  <files>tests/test_backend_contract.py, tests/test_core_backend_integration.py</files>
  <read_first>
    tests/test_analytics.py
    tests/test_qdrant_wrapper.py
    corpulse/db.py
    corpulse/core.py
    .planning/phases/06-storage-foundation/06-RESEARCH.md
    .planning/phases/06-storage-foundation/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `tests/test_backend_contract.py` passes in wave 1 by asserting only the frozen abstract contract, shared row keys, and translated error surface.
    - Test 2: Future parity scenarios for SQLite/in-memory behavior are staged in the test modules with explicit skip fencing until `SQLiteBackend`, backend injection, and `InMemoryBackend` exist.
    - Test 3: `tests/test_core_backend_integration.py` passes in wave 1 by covering the current default `Corpulse()` construction path only, while staging explicit `backend=` cases for activation in later plans.
  </behavior>
  <action>Create `tests/test_backend_contract.py` so wave 1 only exercises what exists after Task 1: imports from `corpulse.backends.base`, assertions on abstract method names/signatures, row TypedDict key sets, and `StorageBackendError` inheritance. Add clearly fenced parity placeholders for SQLite-specific semantics, translated runtime errors, and shared fixture execution using `pytest.mark.skip(reason="Activate in 06-02/06-03 once concrete backends exist")` or an equivalent collection-safe fence. Create `tests/test_core_backend_integration.py` with passing coverage for the current default `Corpulse()` construction path and equally explicit skip-fenced placeholders for `Corpulse(backend=...)`, lifecycle delegation, and the `ValueError` constructor-conflict rule that 06-02 will implement. Keep existing SQLite-private assertions in `tests/test_qdrant_wrapper.py` and `tests/test_analytics.py` untouched here.</action>
  <verify>
    <automated>pytest tests/test_backend_contract.py tests/test_core_backend_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "StorageBackendError|DocumentRow|RetrievalRow|EngagementRow|EmbeddingRow" tests/test_backend_contract.py` finds concrete contract-freezing assertions.
    - `rg -n "skip\\(|pytest\\.mark\\.skip" tests/test_backend_contract.py tests/test_core_backend_integration.py` finds the staged later-wave placeholders.
    - `rg -n "Corpulse\\(\\)" tests/test_core_backend_integration.py` finds the executable default-constructor coverage.
  </acceptance_criteria>
  <done>Wave 1 finishes with passing contract/integration scaffolding only; parity and explicit backend-injection cases are present but fenced until 06-02 and 06-03 provide the required implementation surface.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_backend_contract.py tests/test_core_backend_integration.py -q` after the plan. The suite should prove the frozen contract and default-constructor scaffolding without depending on concrete backend implementations that land later in the phase.
</verification>

<success_criteria>
- The contract is explicit in `corpulse/backends/base.py` and matches the existing eight DB methods.
- Phase 6 has passing backend test scaffolding without rewriting current SQLite-specific regressions or requiring future backend work early.
- Constructor and translated-error expectations are staged in automated tests without making wave 1 depend on unbuilt implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/06-storage-foundation/06-storage-foundation-01-SUMMARY.md`
</output>
