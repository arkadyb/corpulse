# Plan 30-04 - Documentation and Regression Verification

## Metadata

| Field | Value |
| --- | --- |
| Phase | 30 - Workload and Serving Reports |
| Wave | 4 |
| Depends on | 30-03 |
| Requirements | WORK-01, WORK-02, WORK-03, SERV-01, SERV-02 |
| Primary files | `README.md`, `tests/test_workload_reports.py`, `tests/test_trace_jsonl.py`, `tests/test_trace_capture.py`, `tests/test_backend_contract.py`, `tests/test_docstrings.py` |

## Goal

Document the new workload and serving report APIs and run the regression set needed to prove Phase 30 works with existing trace capture and JSONL workload flows.

## Must Haves

- README explains both report methods with minimal examples.
- Documentation states that reports are derived from captured/imported traces and require no LLM-as-judge dependency.
- Regression tests include Phase 29 JSONL import/export and trace capture tests.
- Verification command from 30-VALIDATION.md passes.

## Tasks

<task id="30-04-01" name="Document report APIs">
  <read_first>
    <file>README.md</file>
  </read_first>
  <action>
    Add README documentation for workload_report and serving_report near the existing trace capture/import-export documentation.
  </action>
  <acceptance_criteria>
    <criterion>README includes sync examples for workload_report and serving_report.</criterion>
    <criterion>README describes workload report categories: traffic, tokens, and components.</criterion>
    <criterion>README describes serving report categories: latency distributions, stage latencies, timeout/error rates, and slow contributors.</criterion>
    <criterion>README says reports use captured/imported traces and do not require LLM-as-judge dependencies.</criterion>
  </acceptance_criteria>
</task>

<task id="30-04-02" name="Run focused regression suite">
  <read_first>
    <file>30-VALIDATION.md</file>
  </read_first>
  <action>
    Run the Phase 30 validation command and fix any failures caused by Phase 30 changes.
  </action>
  <acceptance_criteria>
    <criterion>pytest tests/test_workload_reports.py tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q passes.</criterion>
    <criterion>Failures outside Phase 30 changes are documented clearly if they cannot be fixed safely.</criterion>
  </acceptance_criteria>
</task>

<task id="30-04-03" name="Confirm requirements coverage">
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/30-workload-and-serving-reports/30-VALIDATION.md</file>
  </read_first>
  <action>
    Confirm that implementation and tests cover WORK-01, WORK-02, WORK-03, SERV-01, and SERV-02 before marking the phase complete.
  </action>
  <acceptance_criteria>
    <criterion>Each Phase 30 requirement maps to at least one automated test.</criterion>
    <criterion>No session, replay, or model-quality behavior was introduced by mistake.</criterion>
  </acceptance_criteria>
</task>

## Verification

```bash
pytest tests/test_workload_reports.py tests/test_trace_jsonl.py tests/test_trace_capture.py tests/test_backend_contract.py tests/test_docstrings.py -q
```

## Out of Scope

- Additional analytics beyond the roadmap requirements.
- New documentation sites or generated docs.
