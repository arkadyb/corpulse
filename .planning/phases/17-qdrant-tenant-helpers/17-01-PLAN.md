---
phase: 17-qdrant-tenant-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [corpulse/integrations/qdrant.py]
autonomous: true
requirements: [QDRT-HELP-01, QDRT-HELP-02, QDRT-HELP-03, QDRT-HELP-04, QDRT-HELP-05]

must_haves:
  truths:
    - "collection_name_for_user sanitizes IDs to [a-z0-9_]"
    - "chunk_id returns stable UUIDv5 strings"
    - "delete_document_points supports filtered deletion"
    - "ensure_collection handles collection and index idempotency"
    - "Module remains importable without qdrant-client"
  artifacts:
    - path: "corpulse/integrations/qdrant.py"
      provides: "New helper functions"
  key_links:
    - from: "corpulse/integrations/qdrant.py"
      to: "qdrant_client"
      via: "lazy imports inside functions"
---

<objective>
Add additive helper functions to `corpulse/integrations/qdrant.py` for common multi-tenant Qdrant operations: deterministic naming, chunk ID generation, point deletion, and idempotent setup.

Purpose: Reduce boilerplate and enforce best practices for Qdrant multi-tenancy.
Output: Four new public functions in `corpulse.integrations.qdrant`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-qdrant-tenant-helpers/17-RESEARCH.md
@corpulse/integrations/qdrant.py

<interfaces>
From corpulse/integrations/qdrant.py:
```python
def _normalize_points(points, call_kwargs, payload_id_field, payload_filename_key): ...
class QdrantCorpulseClient: ...
class AsyncQdrantCorpulseClient: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic naming and ID helpers</name>
  <files>corpulse/integrations/qdrant.py</files>
  <action>
    Add `collection_name_for_user(user_id, base="corpulse")` and `chunk_id(doc_id, chunk_index)` to `corpulse/integrations/qdrant.py`.
    
    Implementation details:
    - `collection_name_for_user`: Lowercase `user_id`, replace non-`[a-z0-9_]` with `_`, strip leading/trailing `_`. Return `{base}_{sanitized}`. (per QDRT-HELP-01)
    - `chunk_id`: Use `uuid.uuid5` with a fixed `CORPULSE_NAMESPACE = uuid.uuid5(uuid.NAMESPACE_DNS, "corpulse.ai")`. Format input as `f"{doc_id}:{chunk_index}"`. Return as `str`. (per QDRT-HELP-02)
    - Add functions to `__all__`.
  </action>
  <verify>
    <automated>python3 -c "from corpulse.integrations.qdrant import collection_name_for_user, chunk_id; print(collection_name_for_user('User-123!')); print(chunk_id('doc1', 0))"</automated>
  </verify>
  <done>Functions implemented and exportable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement client helpers with lazy imports</name>
  <files>corpulse/integrations/qdrant.py</files>
  <action>
    Add `delete_document_points` and `ensure_collection` to `corpulse/integrations/qdrant.py`.
    
    Implementation details:
    - `delete_document_points(client, collection_name, doc_id, payload_id_field="doc_id")`:
        - If `payload_id_field` is None, use `models.PointIdsList(points=[doc_id])`.
        - Else, use `models.Filter` with `FieldCondition` matching `doc_id` on `payload_id_field`. (per QDRT-HELP-03)
        - Must import `models` inside function (per QDRT-HELP-05).
        - Support both sync/async by returning the result of `client.delete`.
    - `ensure_collection(client, collection_name, vectors_config, payload_indexes=None)`:
        - Check if collection exists via `client.collection_exists`.
        - If not, call `client.create_collection`.
        - For each field in `payload_indexes`, call `client.create_payload_index` with `models.PayloadSchemaType.KEYWORD` (safe default for tenant IDs). (per QDRT-HELP-04)
        - Must import `models` inside function.
        - Support both sync/async.
    - Add functions to `__all__`.
  </action>
  <verify>
    <automated>grep -E "def (delete_document_points|ensure_collection)" corpulse/integrations/qdrant.py</automated>
  </verify>
  <done>Client helpers implemented with lazy imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Collection Name | user_id is sanitized before use in REST path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | collection_name_for_user | mitigate | Strict regex sanitization to [a-z0-9_] |
| T-17-02 | Information Disclosure | delete_document_points | mitigate | Require explicit doc_id/payload_id_field to prevent accidental mass deletion |
</threat_model>

<verification>
Check that `corpulse/integrations/qdrant.py` contains all 4 new functions and they are listed in `__all__`.
Check that no `qdrant_client` imports are at the module level.
</verification>

<success_criteria>
- [ ] `collection_name_for_user` returns sanitized names.
- [ ] `chunk_id` returns deterministic UUID strings.
- [ ] `delete_document_points` uses filters correctly.
- [ ] `ensure_collection` creates collection and indexes idempotently.
- [ ] Lazy imports preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/17-qdrant-tenant-helpers/17-01-SUMMARY.md`
</output>
