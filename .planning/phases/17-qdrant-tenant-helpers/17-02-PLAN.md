---
phase: 17-qdrant-tenant-helpers
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified: [tests/test_qdrant_helpers.py]
autonomous: true
requirements: [QDRT-HELP-01, QDRT-HELP-02, QDRT-HELP-03, QDRT-HELP-04]

must_haves:
  truths:
    - "Sanitization test passes with mixed-case and special characters"
    - "Chunk ID is identical across separate calls for same inputs"
    - "Deletion test removes only specified document in memory client"
    - "Ensure_collection works on first and second calls (idempotency)"
  artifacts:
    - path: "tests/test_qdrant_helpers.py"
      provides: "Tests for new helpers"
  key_links:
    - from: "tests/test_qdrant_helpers.py"
      to: "corpulse.integrations.qdrant"
      via: "import of helper functions"
---

<objective>
Verify the deterministic and idempotency-focused behavior of the new Qdrant helper functions with a comprehensive test suite.

Purpose: Ensure reliability of multi-tenant primitives.
Output: New test file `tests/test_qdrant_helpers.py`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-qdrant-tenant-helpers/17-RESEARCH.md
@corpulse/integrations/qdrant.py
@tests/test_qdrant_wrapper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for deterministic naming and chunk IDs</name>
  <files>tests/test_qdrant_helpers.py</files>
  <action>
    Create `tests/test_qdrant_helpers.py` and implement unit tests for `collection_name_for_user` and `chunk_id`.
    
    Test cases:
    - `collection_name_for_user`:
        - Normal ID: "user1" -> "corpulse_user1"
        - Mixed case: "UserOne" -> "corpulse_userone"
        - Special characters: "user-@#123" -> "corpulse_user_123"
        - Leading/trailing separators: "-user-" -> "corpulse_user"
    - `chunk_id`:
        - Determinism: `chunk_id("doc1", 0)` matches `chunk_id("doc1", 0)`
        - Uniqueness: `chunk_id("doc1", 0)` differs from `chunk_id("doc1", 1)`
        - Stability: Verify against a hardcoded UUID string to ensure the namespace doesn't change.
  </action>
  <verify>
    <automated>pytest tests/test_qdrant_helpers.py -k "collection_name or chunk_id"</automated>
  </verify>
  <done>Deterministic unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for point deletion and idempotent setup</name>
  <files>tests/test_qdrant_helpers.py</files>
  <action>
    Add integration tests to `tests/test_qdrant_helpers.py` using in-memory Qdrant.
    
    Test cases:
    - `delete_document_points`:
        - Create a collection with multiple points, some with `doc_id="abc"`, some with `doc_id="def"`.
        - Call `delete_document_points(client, name, "abc")`.
        - Verify only points with `doc_id="abc"` are gone.
        - Repeat for async client.
    - `ensure_collection`:
        - Call `ensure_collection` for a new collection. Verify it exists and has payload indexes.
        - Call it again with same params. Verify it doesn't raise and remains configured correctly.
        - Repeat for async client.
  </action>
  <verify>
    <automated>pytest tests/test_qdrant_helpers.py</automated>
  </verify>
  <done>Integration tests pass for both sync and async clients.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Data → Helper Functions | Tests use synthetic IDs and data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | tests/test_qdrant_helpers.py | accept | Tests run in isolated environment |
</threat_model>

<verification>
Run `pytest tests/test_qdrant_helpers.py` and ensure all tests pass.
Ensure no `ImportError` occurs when `qdrant-client` is missing (if possible to test in this env, otherwise trust code review).
</verification>

<success_criteria>
- [ ] Sanitization logic verified for edge cases.
- [ ] UUIDv5 stability verified.
- [ ] Point deletion by payload filter verified (sync and async).
- [ ] Idempotent collection creation verified (sync and async).
</success_criteria>

<output>
After completion, create `.planning/phases/17-qdrant-tenant-helpers/17-02-SUMMARY.md`
</output>
