---
phase: 32
plan: 32-01
name: Replay Feasibility Decision Record
wave: 1
depends_on: []
requirements_addressed: [REPLAY-01]
files_modified:
  - .planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md
autonomous: true
---

# Plan 32-01 - Replay Feasibility Decision Record

## Goal

<objective>
Create the human-readable replay design record that decides what Phase 32 will and will not implement.
</objective>

## Must Haves

- Explains why callable replay is feasible now.
- Explains why built-in OpenAI-compatible HTTP replay is deferred.
- Covers timestamp scaling.
- Covers privacy implications of missing raw prompt/message payloads.
- Covers benchmark result export boundaries.
- Does not add code in this plan.

## Tasks

<tasks>
<task id="32-01-01" name="Write replay design record">
  <read_first>
    <file>.planning/phases/32-replay-feasibility-and-minimal-proof/32-RESEARCH.md</file>
    <file>.planning/phases/27-workload-trace-feasibility-and-schema-contract/27-FEASIBILITY.md</file>
    <file>.planning/phases/29-workload-trace-jsonl-import-export/29-RESEARCH.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
  </read_first>
  <action>
    Create `.planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md`.

    The document must contain these exact top-level sections:
    - `# Phase 32 Replay Design: Feasibility and Minimal Proof`
    - `## Decision Summary`
    - `## Current Trace Inputs`
    - `## OpenAI-Compatible Endpoint Replay Boundary`
    - `## Callable Replay Proof`
    - `## Timestamp Scaling`
    - `## Privacy Implications`
    - `## Benchmark Result Export Boundary`
    - `## Phase 32 Implementation Scope`
    - `## Deferred Work`
    - `## Verification Checklist`

    The decision summary must state:
    - `Callable replay is feasible in Phase 32.`
    - `Built-in OpenAI-compatible HTTP replay is deferred.`

    The OpenAI-compatible endpoint section must explain that the current `RagRequestTraceRow` schema does not guarantee a canonical `messages` payload, raw component content, tool payloads, streamed chunks, or response bodies.

    The timestamp section must define:
    - default replay has no sleeping
    - `time_scale=1.0` means real-time captured deltas
    - `time_scale>1.0` means faster replay
    - `max_delay_seconds` caps long gaps

    The benchmark section must state that Phase 32 does not persist replay results or export benchmark summaries.
  </action>
  <acceptance_criteria>
    <criterion>`test -f .planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md` exits 0.</criterion>
    <criterion>`rg "Callable replay is feasible in Phase 32|Built-in OpenAI-compatible HTTP replay is deferred" .planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md` exits 0.</criterion>
    <criterion>`rg "canonical .*messages|raw component content|tool payloads|streamed chunks|response bodies" .planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md` exits 0.</criterion>
    <criterion>`rg "time_scale=1\\.0|time_scale>1\\.0|max_delay_seconds|does not persist replay results|export benchmark summaries" .planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md` exits 0.</criterion>
  </acceptance_criteria>
</task>
</tasks>

## Verification

```bash
rg "Callable replay is feasible in Phase 32|Built-in OpenAI-compatible HTTP replay is deferred|time_scale=1\\.0|max_delay_seconds|export benchmark summaries" .planning/phases/32-replay-feasibility-and-minimal-proof/32-REPLAY-DESIGN.md
```

## Out of Scope

- Replay code.
- Public APIs.
- README documentation.
