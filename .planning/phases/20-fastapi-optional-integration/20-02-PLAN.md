---
phase: 20-fastapi-optional-integration
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [tests/test_fastapi.py]
autonomous: true
requirements: [FASTAPI-04]
must_haves:
  truths:
    - "Integration tests prove all FastAPI endpoints return 200 OK with mock backend"
    - "Tests verify response structure matches expected fixtures"
    - "Tests handle AsyncCorpulse dependency correctly"
    - "Tests are skipped gracefully if fastapi or httpx are missing"
  artifacts:
    - path: "tests/test_fastapi.py"
      provides: "FastAPI integration coverage"
      min_lines: 60
  key_links:
    - from: "tests/test_fastapi.py"
      to: "corpulse/fastapi.py"
      via: "router instantiation"
---

<objective>
Implement integration tests for the FastAPI router to ensure all endpoints correctly interact with `AsyncCorpulse` and return the expected typed payloads.

Purpose: Guarantee the reliability of the FastAPI integration and provide a working example of how to use the router factory.
Output: New test file `tests/test_fastapi.py`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@corpulse/fastapi.py
@tests/test_async_core_integration.py
@tests/report_fixtures.py

<interfaces>
From tests/test_async_core_integration.py:
```python
class FakeAsyncBackend:
    # Implements upsert_document, insert_retrieval, etc.
    # Can be seeded with documents and retrieval_rows.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_fastapi.py and implement integration tests</name>
  <files>tests/test_fastapi.py</files>
  <action>
    Create `tests/test_fastapi.py`.
    - Include a guard at the top to skip the entire module if `fastapi` or `httpx` is missing (`pytest.skip(..., allow_module_level=True)`).
    - Import `FastAPI`, `AsyncClient` (from `httpx`), and `get_corpulse_router`.
    - Implement a `FakeAsyncBackend` (or reuse logic from `tests/test_async_core_integration.py`) and seed it with a minimal test corpus.
    - Create a `get_test_corpulse` dependency that yields an `AsyncCorpulse(backend=fake_backend)`.
    - Initialize a `FastAPI` app and include the router from `get_corpulse_router(get_test_corpulse)`.
    - Implement async test functions for each route:
      - `test_get_report`: Verify 200 OK and summary fields.
      - `test_get_cleanup_report`: Verify 200 OK and section counts.
      - `test_get_ghosts`: Verify 200 OK and list content.
      - `test_get_duplicates`: Verify 200 OK (and handle sklearn presence).
      - `test_get_obsolete`: Verify 200 OK.
      - `test_get_stale`: Verify 200 OK.
      - `test_get_suspects`: Verify 200 OK.
    - Assert that the JSON response keys match the `TypedDict` models from `corpulse.models`.
  </action>
  <verify>
    <automated>pytest tests/test_fastapi.py</automated>
  </verify>
  <done>Integration tests are implemented and passing (or skipped if dependencies missing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A (Testing phase)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Elevation of Privilege | Test dependencies | accept | Tests run in a controlled environment. |
</threat_model>

<verification>
Run `pytest tests/test_fastapi.py`. If dependencies are missing, the skip message should be visible. If they are present, all tests should pass.
</verification>

<success_criteria>
- `tests/test_fastapi.py` covers all 7 analysis endpoints.
- Tests use `AsyncClient` for true integration verification.
- Tests verify response payloads against expected dictionary structures.
- Tests prove that `AsyncCorpulse` dependency injection works as intended.
</success_criteria>

<output>
After completion, create `.planning/phases/20-fastapi-optional-integration/20-02-SUMMARY.md`
</output>
