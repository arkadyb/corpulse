---
phase: 28
plan: 01
title: Workload trace models and in-memory backend
type: execute
wave: 1
depends_on: []
files_modified:
  - corpulse/models.py
  - corpulse/backends/base.py
  - corpulse/backends/memory.py
  - tests/test_trace_capture.py
autonomous: true
requirements: [TRACE-01, TRACE-02, TRACE-03, TRACE-04]
requirements_addressed: [TRACE-01, TRACE-02, TRACE-03, TRACE-04]
---

<objective>
Add the workload trace data model, storage contract, and in-memory implementation that later backend/API plans build on.
</objective>

<truths>
- Use the Phase 27 append-only schema decision exactly.
- Do not modify SQLite, Postgres, async Postgres, public facades, or docs in this plan.
- Existing generation trace behavior must remain unchanged.
</truths>

<threat_model>
This plan changes in-process data structures only. Sensitive data risk is controlled by making raw query text optional and supporting hashes/references in component records. Do not require raw component content in models or tests.
</threat_model>

<tasks>
<task id="28-01-T1" type="execute">
<title>Add workload trace model types</title>
<read_first>
- corpulse/models.py
- .planning/phases/27-workload-trace-feasibility-and-schema-contract/27-FEASIBILITY.md
- .planning/phases/28-workload-trace-capture-foundation/28-PATTERNS.md
</read_first>
<action>
In `corpulse/models.py`, add `RagRequestComponent`, `RagRequestTimings`, and `RagRequestTraceRow` `TypedDict` classes near `GenerationTraceRow`.

`RagRequestComponent` must include:
- `type: str`
- `token_count: int | None`
- `refs: list[dict[str, Any]] | None`
- `content_hash: str | None`
- `metadata: dict[str, Any] | None`

`RagRequestTimings` must include optional float fields:
- `ttft_ms`
- `tpot_ms`
- `retrieval_ms`
- `rerank_ms`
- `generation_ms`
- `queue_ms`
- `total_latency_ms`

`RagRequestTraceRow` must include:
- `trace_id: int`
- `request_id: str | None`
- `session_id: str | None`
- `query_text: str | None`
- `query_hash: str | None`
- `input_token_count: int | None`
- `output_token_count: int | None`
- `components: list[RagRequestComponent]`
- `timings: RagRequestTimings`
- `timeout: bool`
- `error: str | None`
- `captured_at: float`
</action>
<acceptance_criteria>
- `rg "class RagRequestComponent\\(TypedDict\\)" corpulse/models.py` exits 0.
- `rg "class RagRequestTimings\\(TypedDict" corpulse/models.py` exits 0.
- `rg "class RagRequestTraceRow\\(TypedDict\\)" corpulse/models.py` exits 0.
- `rg "trace_id: int|request_id: str \\| None|session_id: str \\| None|components: list\\[RagRequestComponent\\]|captured_at: float" corpulse/models.py` exits 0.
</acceptance_criteria>
</task>

<task id="28-01-T2" type="execute">
<title>Add backend contract methods</title>
<read_first>
- corpulse/backends/base.py
- corpulse/models.py
</read_first>
<action>
In `corpulse/backends/base.py`, import `RagRequestComponent`, `RagRequestTimings`, and `RagRequestTraceRow`.

Add abstract method `insert_rag_request_trace(...) -> None` with parameters:
- `request_id: str | None`
- `session_id: str | None`
- `query_text: str | None`
- `query_hash: str | None`
- `input_token_count: int | None`
- `output_token_count: int | None`
- `components: list[RagRequestComponent]`
- `timings: RagRequestTimings`
- `timeout: bool`
- `error: str | None`
- `captured_at: float`

Add abstract method `rag_request_traces(self, since: float) -> list[RagRequestTraceRow]`.
</action>
<acceptance_criteria>
- `rg "RagRequestComponent|RagRequestTimings|RagRequestTraceRow" corpulse/backends/base.py` exits 0.
- `rg "def insert_rag_request_trace" corpulse/backends/base.py` exits 0.
- `rg "def rag_request_traces\\(self, since: float\\) -> list\\[RagRequestTraceRow\\]" corpulse/backends/base.py` exits 0.
</acceptance_criteria>
</task>

<task id="28-01-T3" type="execute">
<title>Implement in-memory workload trace storage</title>
<read_first>
- corpulse/backends/memory.py
- corpulse/models.py
- tests/test_trace_capture.py
</read_first>
<action>
In `corpulse/backends/memory.py`:
- import `RagRequestComponent`, `RagRequestTimings`, and `RagRequestTraceRow`
- add `self._rag_request_traces: list[dict[str, object]] = []` in `__init__`
- implement `insert_rag_request_trace(...)` using the exact contract from `StorageBackend`
- implement `rag_request_traces(self, since: float) -> list[RagRequestTraceRow]`
- copy nested `components`, `refs`, `metadata`, and `timings` structures on write/read so callers cannot mutate stored rows
- sort reads by `(captured_at, trace_id)`

In `tests/test_trace_capture.py`, add a focused in-memory test named `test_rag_request_trace_in_memory_round_trip_is_ordered_and_append_only`.
</action>
<acceptance_criteria>
- `rg "_rag_request_traces|def insert_rag_request_trace|def rag_request_traces" corpulse/backends/memory.py` exits 0.
- `rg "test_rag_request_trace_in_memory_round_trip_is_ordered_and_append_only" tests/test_trace_capture.py` exits 0.
- `pytest tests/test_trace_capture.py -q` exits 0.
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
pytest tests/test_trace_capture.py -q
pytest tests/test_backend_contract.py -q
```
</verification>

<success_criteria>
1. Workload trace models exist.
2. StorageBackend defines explicit workload trace methods.
3. InMemoryBackend can round-trip workload traces deterministically.
4. Existing generation trace tests still pass.
</success_criteria>

<must_haves>
- TRACE-01, TRACE-02, TRACE-03, and TRACE-04 remain visible in this plan because the storage contract must support the full capture shape.
</must_haves>
