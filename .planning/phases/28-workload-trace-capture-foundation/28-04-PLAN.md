---
phase: 28
plan: 04
title: Compatibility verification and user guidance
type: execute
wave: 4
depends_on: [28-01, 28-02, 28-03]
files_modified:
  - README.md
  - tests/test_import.py
  - tests/test_package.py
  - tests/test_report_helpers.py
  - tests/test_qdrant_wrapper.py
autonomous: true
requirements: [TRACE-04, TRACE-01]
requirements_addressed: [TRACE-04, TRACE-01]
---

<objective>
Document the new workload trace capture surface and verify that existing corpulse behavior remains compatible.
</objective>

<truths>
- This plan depends on all implementation plans.
- Do not introduce new required dependencies.
- Keep workload tracing additive and library-first.
</truths>

<threat_model>
Documentation can create unsafe expectations. State that raw query/component content is optional and that hashes/references are supported for privacy-preserving traces.
</threat_model>

<tasks>
<task id="28-04-T1" type="execute">
<title>Document workload trace capture</title>
<read_first>
- README.md
- .planning/phases/27-workload-trace-feasibility-and-schema-contract/27-FEASIBILITY.md
- corpulse/core.py
- corpulse/async_core.py
</read_first>
<action>
Add a concise README section named `## Workload Trace Capture` that shows:
- `corp.log_rag_request(...)`
- `session_id="session-123"`
- component examples using `system_prompt`, `vector_db`, `chat_history`, and `user_input`
- `timings={"ttft_ms": 210, "tpot_ms": 18, "retrieval_ms": 42}`
- a note that raw query/component content is optional and hashes/references are supported
- `await async_corp.alog_rag_request(...)`
</action>
<acceptance_criteria>
- `rg "## Workload Trace Capture|log_rag_request|alog_rag_request|session-123|ttft_ms|tpot_ms|raw query" README.md` exits 0.
</acceptance_criteria>
</task>

<task id="28-04-T2" type="execute">
<title>Run compatibility-focused tests</title>
<read_first>
- tests/test_import.py
- tests/test_package.py
- tests/test_report_helpers.py
- tests/test_qdrant_wrapper.py
- tests/test_trace_capture.py
</read_first>
<action>
Run compatibility tests and fix only regressions caused by workload trace changes:

```bash
pytest tests/test_import.py tests/test_package.py tests/test_report_helpers.py tests/test_qdrant_wrapper.py tests/test_trace_capture.py -q
```

If failures are unrelated pre-existing environmental skips, record them in the plan summary rather than changing unrelated code.
</action>
<acceptance_criteria>
- `pytest tests/test_import.py tests/test_package.py tests/test_report_helpers.py tests/test_qdrant_wrapper.py tests/test_trace_capture.py -q` exits 0 or skips optional dependency tests only for missing optional packages.
</acceptance_criteria>
</task>
</tasks>

<verification>
```bash
pytest tests/test_trace_capture.py tests/test_backend_contract.py tests/test_import.py tests/test_package.py tests/test_report_helpers.py tests/test_qdrant_wrapper.py -q
```
</verification>

<success_criteria>
1. README documents workload trace capture.
2. Compatibility-focused tests pass.
3. TRACE-04 is satisfied by preserved existing behavior.
</success_criteria>

<must_haves>
- Existing retrieval, engagement, generation trace, report, cleanup, wrapper, and import behavior remains compatible.
- README explains the new TRACE-01 capture API without implying raw content is required.
</must_haves>
