---
phase: 09-harden-sync-postgres-backend
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md
  - .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - BACK-04
  - INT-03
gap_closure: true
user_setup:
  - service: postgres-test-db
    why: "Needed only if live Postgres verification evidence is being refreshed in the same execution run"
    env_vars:
      - name: CORPULSE_POSTGRES_TEST_CONNINFO
        source: "Reachable PostgreSQL test database connection string"
must_haves:
  truths:
    - "Phase 7 no longer has stale verification evidence claiming Postgres live verification is still pending after the sync backend work is complete."
    - "Phase 9 has a current verification artifact proving sync Postgres pooling and parity coverage for the production path with a passed live pooled-Postgres run."
    - "Roadmap and requirements traceability show `BACK-04` and `INT-03` closed by verified evidence, not code-only claims."
  artifacts:
    - path: ".planning/phases/07-postgresbackend-sync/07-VERIFICATION.md"
      provides: "Refreshed verification report for the original sync Postgres deliverable"
    - path: ".planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md"
      provides: "Phase 9 verification report tying pooling behavior to automated evidence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes and traceability for BACK-04 and INT-03"
  key_links:
    - from: ".planning/phases/07-postgresbackend-sync/07-VERIFICATION.md"
      to: "tests/test_postgres_backend.py"
      via: "current sync Postgres evidence and live-proof command/status"
      pattern: "test_postgres_backend.py|CORPULSE_POSTGRES_TEST_CONNINFO"
    - from: ".planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md"
      to: "corpulse/backends/postgres.py"
      via: "explicit proof that the backend now pools sync connections"
      pattern: "ConnectionPool|pooling|self\\._conn"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "traceability for BACK-04 and INT-03"
      pattern: "BACK-04|INT-03"
---

<objective>
Turn the sync Postgres backend changes into milestone-grade verification evidence and traceability so the Phase 7 stale proof gap and the milestone `INT-03` gap are both closed on disk.

Purpose: convert the code fix from Plan 01 into auditable artifacts that the milestone audit can trust.
Output: refreshed `07-VERIFICATION.md`, new `09-VERIFICATION.md`, and updated roadmap/requirements traceability for `BACK-04` and `INT-03`.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-v1.1-MILESTONE-AUDIT.md
@.planning/phases/07-postgresbackend-sync/07-VERIFICATION.md
@.planning/phases/07-postgresbackend-sync/07-postgresbackend-sync-01-SUMMARY.md
@.planning/phases/09-harden-sync-postgres-backend/09-01-PLAN.md
@corpulse/backends/postgres.py
@tests/test_postgres_backend.py
@tests/conftest.py
@tests/test_backend_contract.py
@tests/test_core_backend_integration.py

<interfaces>
Evidence this plan must preserve:
```text
BACK-04 = sync Postgres backend via psycopg with schema auto-init
INT-03 = PostgresBackend and AsyncPostgresBackend support connection pooling
```

Required verification inputs after Plan 01:
```text
pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_package.py -q
CORPULSE_POSTGRES_TEST_CONNINFO=... pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py -q
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh sync Postgres verification artifacts with current pooling evidence</name>
  <files>.planning/phases/07-postgresbackend-sync/07-VERIFICATION.md, .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md</files>
  <read_first>
    .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md
    .planning/v1.1-v1.1-MILESTONE-AUDIT.md
    .planning/phases/07-postgresbackend-sync/07-postgresbackend-sync-01-SUMMARY.md
    .planning/phases/09-harden-sync-postgres-backend/09-01-PLAN.md
    corpulse/backends/postgres.py
    tests/test_postgres_backend.py
    tests/test_backend_contract.py
    tests/test_core_backend_integration.py
  </read_first>
  <acceptance_criteria>
    - `rg -n "status: passed" .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` finds explicit passed status lines in both reports.
    - `rg -n "Command: .*pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py -q|Exit status: 0|Observed result: live pooled Postgres parity passed|Executed: [0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` finds command, execution date, exit status, and observed-result traceability for the required live run.
    - `rg -n "Command: .*pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_package.py -q|Exit status: 0|Observed result: deterministic sync Postgres pooling checks passed" .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` finds deterministic run traceability in the verification artifacts.
    - `rg -n "ConnectionPool|pooling|self\\._conn" .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` finds the sync pooling evidence narrative.
    - `test -f .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` exits 0.
  </acceptance_criteria>
  <action>Require a real pooled PostgreSQL verification run before this task can complete. First, fail fast if `CORPULSE_POSTGRES_TEST_CONNINFO` is unset; do not downgrade the requirement closure to documentation-only progress. Re-run both sync Postgres commands from Plan 01 using actual execution output: the deterministic command `pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_package.py -q` and the live pooled command `CORPULSE_POSTGRES_TEST_CONNINFO=... pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py -q`. Rewrite `.planning/phases/07-postgresbackend-sync/07-VERIFICATION.md` so it no longer reports stale pre-pooling evidence and instead records both command results with grep-verifiable fields for each run: `Executed: YYYY-MM-DD`, `Command: ...`, `Exit status: 0`, and `Observed result: ...`. Create `.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` with milestone-grade structure matching other phase verification reports and include the same explicit execution-evidence fields for deterministic and live pooled runs. Both reports must state that `corpulse/backends/postgres.py` now uses `psycopg_pool.ConnectionPool`, that the sync `Corpulse` facade is unchanged, and that the production Postgres path passed with a live pooled backend before `BACK-04` and `INT-03` are considered closed.</action>
  <verify>
    <automated>test -n "$CORPULSE_POSTGRES_TEST_CONNINFO" && pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_package.py -q && CORPULSE_POSTGRES_TEST_CONNINFO="$CORPULSE_POSTGRES_TEST_CONNINFO" pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py -q && test -f .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md</automated>
  </verify>
  <done>The original Phase 7 verification report is refreshed with current deterministic and live pooled-Postgres evidence, and Phase 9 has its own verification artifact documenting sync pooling closure with command/output traceability for the milestone audit.</done>
</task>

<task type="auto">
  <name>Task 2: Update roadmap and requirement traceability for BACK-04 and INT-03</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/v1.1-v1.1-MILESTONE-AUDIT.md
    .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md
    .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - `rg -n "\\[x\\] \\*\\*BACK-04\\*\\*|\\[x\\] \\*\\*INT-03\\*\\*" .planning/REQUIREMENTS.md` finds both requirement checkboxes marked complete.
    - `rg -n "\\| BACK-04 \\| Phase 9 \\| Complete \\||\\| INT-03 \\| Phase 9 \\| Complete \\|" .planning/REQUIREMENTS.md` finds the updated traceability rows.
    - `rg -n "^\\*\\*Plans:\\*\\* 0/2 plans complete$" .planning/ROADMAP.md` finds the actual Phase 9 roadmap plan-count line.
    - `rg -n "^- \\[ \\] 09-01-PLAN.md|^- \\[ \\] 09-02-PLAN.md" .planning/ROADMAP.md` finds the actual Phase 9 plan entries.
  </acceptance_criteria>
  <action>Update `.planning/REQUIREMENTS.md` so `BACK-04` and `INT-03` are checked off only after Task 1 has produced passed verification artifacts showing both deterministic and live pooled-Postgres runs with `Exit status: 0`. Change the traceability table to map both requirements to `Phase 9` with `Complete` status only after those passed artifacts exist. Keep `.planning/ROADMAP.md` aligned with the actual Phase 9 plan list and plan-count syntax already used in the file: `**Plans**: 0/2 plans complete` plus the `09-01-PLAN.md` and `09-02-PLAN.md` entries. Do not mark Phase 10 complete or alter its async-facade scope; this task closes the sync backend milestone evidence only after live pooled verification has passed.</action>
  <verify>
    <automated>rg -n "\\[x\\] \\*\\*BACK-04\\*\\*|\\[x\\] \\*\\*INT-03\\*\\*" .planning/REQUIREMENTS.md && rg -n "^\\*\\*Plans:\\*\\* 0/2 plans complete$|^- \\[ \\] 09-01-PLAN.md|^- \\[ \\] 09-02-PLAN.md" .planning/ROADMAP.md && rg -n "status: passed|Exit status: 0|Observed result: live pooled Postgres parity passed" .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md</automated>
  </verify>
  <done>Requirement and roadmap traceability on disk now matches the verified sync Postgres reality: `BACK-04` and `INT-03` are closed only after live pooled-Postgres evidence exists, and the milestone no longer depends on stale Phase 7 proof.</done>
</task>

</tasks>

<verification>
Run `test -n "$CORPULSE_POSTGRES_TEST_CONNINFO"`, then `pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py tests/test_package.py -q`, then `CORPULSE_POSTGRES_TEST_CONNINFO="$CORPULSE_POSTGRES_TEST_CONNINFO" pytest tests/test_postgres_backend.py tests/test_backend_contract.py tests/test_core_backend_integration.py -q`. Only after both commands pass and both verification reports record command/date/exit-status/observed-result fields should the requirement and roadmap grep checks be applied.
</verification>

<success_criteria>
- `.planning/phases/07-postgresbackend-sync/07-VERIFICATION.md` is no longer stale.
- `.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` proves sync pooling and parity coverage with current deterministic and live pooled evidence.
- `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` show `BACK-04` and `INT-03` closed only after verified Phase 9 live pooled-Postgres work.
</success_criteria>

<output>
After completion, create `.planning/phases/09-harden-sync-postgres-backend/09-harden-sync-postgres-backend-02-SUMMARY.md`
</output>
