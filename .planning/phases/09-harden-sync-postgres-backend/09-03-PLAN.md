---
phase: 09-harden-sync-postgres-backend
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - BACK-04
  - INT-03
gap_closure: true
must_haves:
  truths:
    - "`BACK-04` remains closed in Phase 9 with the existing verified sync Postgres evidence."
    - "`INT-03` is no longer presented as fully closed by Phase 9's sync-only verification artifact."
    - "Roadmap and requirements traceability send async pooling closure to later async evidence work instead of fabricating proof inside Phase 9."
  artifacts:
    - path: ".planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md"
      provides: "Phase 9 verification report scoped to verified sync Postgres evidence and explicit INT-03 remap"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement status and traceability that preserve BACK-04 complete while reopening or remapping INT-03"
    - path: ".planning/ROADMAP.md"
      provides: "Phase ownership and plan list that no longer claim full INT-03 closure in Phase 9"
  key_links:
    - from: ".planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "verification coverage wording for BACK-04 versus INT-03"
      pattern: "BACK-04|INT-03|sync half|remap|pending"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "traceability row and phase requirement ownership"
      pattern: "INT-03|BACK-04|Phase 9|Phase 10|Pending"
---

<objective>
Narrow Phase 9's requirement claim to the verified sync Postgres work and move async pooling closure out of this phase so the milestone traceability stops overstating what has been proven.

Purpose: resolve the verification gap without inventing async evidence or adding new backend implementation work to Phase 9.
Output: updated Phase 9 verification wording plus roadmap and requirements traceability that keep `BACK-04` closed and reopen or remap `INT-03`.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md
@.planning/phases/09-harden-sync-postgres-backend/09-02-PLAN.md
@.planning/phases/09-harden-sync-postgres-backend/09-harden-sync-postgres-backend-02-SUMMARY.md
@.planning/phases/08-asyncpostgresbackend/08-asyncpostgresbackend-01-PLAN.md
@.planning/phases/07-postgresbackend-sync/07-VERIFICATION.md

<interfaces>
Requirement text from `.planning/REQUIREMENTS.md`:
```text
BACK-04 = PostgresBackend (sync) via psycopg>=3.2 with schema auto-init
INT-03 = PostgresBackend and AsyncPostgresBackend support connection pooling
```

Current verification constraint from `.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md`:
```text
Phase 9 evidence covers sync pooling only; it does not close the full
`PostgresBackend and AsyncPostgresBackend support connection pooling` requirement.
```

Phase constraint for this plan:
```text
Do not add async implementation work to Phase 9.
Preferred fix: reopen or remap INT-03 rather than fabricate async proof.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rescope the Phase 9 verification artifact to verified sync evidence only</name>
  <files>.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md
    .planning/phases/09-harden-sync-postgres-backend/09-02-PLAN.md
    .planning/phases/09-harden-sync-postgres-backend/09-harden-sync-postgres-backend-02-SUMMARY.md
    .planning/phases/08-asyncpostgresbackend/08-asyncpostgresbackend-01-PLAN.md
    .planning/phases/07-postgresbackend-sync/07-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>Rewrite `.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` so the failed truth and requirements coverage explicitly preserve `BACK-04` as satisfied by the recorded sync pooling evidence while stating that `INT-03` is not closed here because this artifact proves only the sync half. Remove any wording that says Phase 9 closes full `INT-03`, and replace it with explicit remap language pointing async pooling evidence to later async verification work rather than Phase 9. Update `.planning/REQUIREMENTS.md` in the same change so `BACK-04` stays checked complete, `INT-03` is reopened to unchecked status, and the traceability row no longer maps `INT-03` to `Phase 9 | Complete`. Use a pending/remapped mapping that still preserves the already-verified sync contribution, for example `Phases 9-10 | Pending`, rather than fabricating a completed async proof.</action>
  <verify>
    <automated>rg -n "BACK-04.*SATISFIED|BACK-04.*PASSED|INT-03.*BLOCKED|INT-03.*Pending|sync half|remap|async evidence" .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BACK-04" .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` shows `BACK-04` still covered by passed sync evidence.
    - `rg -n "INT-03.*(BLOCKED|Pending|Pending async evidence|Phases 9-10)" .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md .planning/REQUIREMENTS.md` shows `INT-03` no longer marked complete in Phase 9.
    - `rg -n "^- \\[x\\] \\*\\*BACK-04\\*\\*" .planning/REQUIREMENTS.md` finds `BACK-04` still complete.
    - `rg -n "^- \\[ \\] \\*\\*INT-03\\*\\*" .planning/REQUIREMENTS.md` finds `INT-03` reopened.
    - `rg -n "\\| INT-03 \\| (Phases 9-10|Phase 10) \\| Pending \\|" .planning/REQUIREMENTS.md` finds the remapped traceability row.
  </acceptance_criteria>
  <done>Phase 9's verification artifact now makes only the claim the evidence supports, and requirement traceability preserves verified sync completion without pretending async pooling has already been proven.</done>
</task>

<task type="auto">
  <name>Task 2: Update roadmap ownership and plan status so Phase 9 stops claiming full closure</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md
    .planning/phases/08-asyncpostgresbackend/08-asyncpostgresbackend-01-PLAN.md
  </read_first>
  <action>Update `.planning/ROADMAP.md` so the Phase 9 section no longer implies that all work for `INT-03` is complete here. Keep `BACK-04` owned by Phase 9, remove full `INT-03` closure from the Phase 9 requirement line, and move responsibility for final async pooling evidence into the async follow-up phase by adding `INT-03` to Phase 10's requirement line. Refresh the Phase 9 plan list to `**Plans**: 2/3 plans complete` and add `09-03-PLAN.md` with a brief objective about remapping `INT-03` traceability. Update the roadmap progress table row for Phase 9 to show it is not fully complete while this gap-closure plan is pending; do not mark Phase 10 complete and do not add any async implementation scope to Phase 9.</action>
  <verify>
    <automated>rg -n "^\\*\\*Requirements\\*\\*: BACK-04$|^\\*\\*Requirements\\*\\*: BACK-05, INT-03$|^\\*\\*Plans\\*\\*: 2/3 plans complete$|^- \\[ \\] 09-03-PLAN.md|\\| 9\\. Harden Sync Postgres Backend \\| v1\\.1 \\| 2/3 \\| In progress \\| - \\|" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^\\*\\*Requirements\\*\\*: BACK-04$" .planning/ROADMAP.md` finds the Phase 9 requirement line narrowed to sync scope.
    - `rg -n "^\\*\\*Requirements\\*\\*: BACK-05, INT-03$" .planning/ROADMAP.md` finds the Phase 10 requirement line updated to own final async pooling closure.
    - `rg -n "^\\*\\*Plans\\*\\*: 2/3 plans complete$" .planning/ROADMAP.md` finds the Phase 9 plan count updated.
    - `rg -n "^- \\[ \\] 09-03-PLAN.md — Reopen or remap INT-03 traceability so Phase 9 claims only verified sync closure$" .planning/ROADMAP.md` finds the new Phase 9 plan entry.
    - `rg -n "\\| 9\\. Harden Sync Postgres Backend \\| v1\\.1 \\| 2/3 \\| In progress \\| - \\|" .planning/ROADMAP.md` finds the progress table no longer claiming full completion.
  </acceptance_criteria>
  <done>The roadmap now matches the verification reality: Phase 9 remains the verified sync closure work, and the outstanding async side of `INT-03` is visibly owned by later async evidence work.</done>
</task>

</tasks>

<verification>
Run the grep checks in both tasks. The result should show `BACK-04` still complete, `INT-03` reopened or remapped out of Phase 9 completion, the new `09-03-PLAN.md` entry present in the Phase 9 plan list, and the roadmap progress row no longer showing Phase 9 as fully complete.
</verification>

<success_criteria>
- `.planning/phases/09-harden-sync-postgres-backend/09-VERIFICATION.md` no longer claims full `INT-03` closure from sync-only evidence.
- `.planning/REQUIREMENTS.md` preserves `BACK-04` complete and reopens or remaps `INT-03`.
- `.planning/ROADMAP.md` shows the new pending Phase 9 gap-closure plan and hands final async pooling closure to the async follow-up phase.
</success_criteria>

<output>
After completion, create `.planning/phases/09-harden-sync-postgres-backend/09-harden-sync-postgres-backend-03-SUMMARY.md`
</output>
