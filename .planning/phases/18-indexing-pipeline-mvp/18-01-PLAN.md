---
phase: 18-indexing-pipeline-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [corpulse/pipelines/indexing.py]
autonomous: true
requirements: [PIPE-04]
must_haves:
  truths:
    - "IndexingResult dataclass exists with correct fields"
    - "Parser, Chunker, and Embedder protocols are defined"
    - "index_document has the correct signature"
  artifacts:
    - path: "corpulse/pipelines/indexing.py"
      provides: "Indexing protocols and result contract"
  key_links: []
---

<objective>
Define indexing protocols and minimal result contract in `corpulse/pipelines/indexing.py`.

Purpose: Provide a provider-agnostic interface for parsing, chunking, and embedding documents.
Output: `IndexingResult` dataclass and `Parser`, `Chunker`, `Embedder` protocols.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-indexing-pipeline-mvp/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IndexingResult and Protocols</name>
  <files>corpulse/pipelines/indexing.py</files>
  <action>
Create `corpulse/pipelines/indexing.py` and define the following components:
- `IndexingResult`: A frozen dataclass with `doc_id` (str), `chunk_count` (int), and `duration_ms` (float) per PIPE-04.
- `Parser` (Protocol): `async def parse(source: Any) -> str`.
- `Chunker` (Protocol): `async def chunk(text: str) -> list[str]`.
- `Embedder` (Protocol): `async def embed(chunks: list[str]) -> list[list[float]]`.
Use `typing.Protocol` and `typing.runtime_checkable` for the protocols.
  </action>
  <verify>
    <automated>python3 -c "from corpulse.pipelines.indexing import IndexingResult, Parser, Chunker, Embedder; print('OK')"</automated>
  </verify>
  <done>Dataclass and Protocols are defined and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Define index_document signature</name>
  <files>corpulse/pipelines/indexing.py</files>
  <action>
Add a skeleton implementation of `index_document` to `corpulse/pipelines/indexing.py`:
- Parameters: `doc_id`, `filename`, `source`, `collection_name`, `client` (AsyncQdrantClient), `corpulse` (AsyncCorpulse), `parser`, `chunker`, `embedder`, and an optional `vector_name`.
- Documentation: Add a clear docstring describing the orchestration flow (parse -> chunk -> embed -> Qdrant upsert -> Corpulse register).
- Implementation: Just initialize a timer and return a dummy `IndexingResult` for now.
  </action>
  <verify>
    <automated>python3 -c "from corpulse.pipelines.indexing import index_document; import asyncio; asyncio.run(index_document('d1', 'f1', 'src', 'coll', None, None, None, None, None))" | grep -v "Error"</automated>
  </verify>
  <done>Function signature is defined with correct type hints and parameters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → External Services | Chunker/Embedder/Parser may call external APIs (untrusted inputs). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | S | `index_document` | mitigate | Ensure `doc_id` and `collection_name` are passed correctly to backend helpers. |
| T-18-02 | D | `embed` calls | mitigate | Timeouts and retries for external LLM calls (Wave 2). |
</threat_model>

<verification>
Check that `corpulse/pipelines/indexing.py` contains the expected classes and the function signature matches the research recommendations.
</verification>

<success_criteria>
`IndexingResult` and the three protocols are defined and functional. `index_document` has the correct signature.
</success_criteria>

<output>
After completion, create `.planning/phases/18-indexing-pipeline-mvp/18-01-SUMMARY.md`
</output>
