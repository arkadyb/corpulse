---
phase: 18-indexing-pipeline-mvp
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified: [tests/test_indexing_pipeline.py]
autonomous: true
requirements: [PIPE-05]
must_haves:
  truths:
    - "Pipeline happy path test passes with fakes"
    - "Retry logic is verified by failing the first embedding call"
    - "Rollback logic is verified by failing the Corpulse registration"
  artifacts:
    - path: "tests/test_indexing_pipeline.py"
      provides: "Unit and integration tests for indexing pipeline"
  key_links: []
---

<objective>
Implement comprehensive pipeline tests using fakes and mocks in `tests/test_indexing_pipeline.py`.

Purpose: Verify the pipeline's correctness, resiliency (retries), and consistency (rollback) without requiring external services.
Output: Unit and integration tests for `index_document`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-indexing-pipeline-mvp/18-RESEARCH.md
@corpulse/pipelines/indexing.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pipeline Fakes and Happy Path Test</name>
  <files>tests/test_indexing_pipeline.py</files>
  <action>
Create `tests/test_indexing_pipeline.py` and implement the following:
- Define `FakeParser`, `FakeChunker`, and `FakeEmbedder` that implement the indexing protocols.
- Implement a `test_indexing_pipeline_happy_path` that uses `AsyncMock` for `AsyncQdrantClient` and `AsyncCorpulse`.
- Verify that `index_document` calls all components in the correct order and returns an `IndexingResult` with valid fields (PIPE-01, PIPE-05).
  </action>
  <verify>
    <automated>pytest tests/test_indexing_pipeline.py -k test_happy_path</automated>
  </verify>
  <done>Happy path test verifies the full orchestration flow with fakes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Retry and Rollback Tests</name>
  <files>tests/test_indexing_pipeline.py</files>
  <action>
Extend `tests/test_indexing_pipeline.py` with the following tests:
- `test_indexing_pipeline_embed_retry`: Configure `FakeEmbedder` to fail once and then succeed. Verify the pipeline succeeds and makes multiple calls.
- `test_indexing_pipeline_embed_retry_failure`: Configure `FakeEmbedder` to fail always. Verify the pipeline raises the final exception after max retries.
- `test_indexing_pipeline_rollback`: Mock `AsyncCorpulse.register_document` to raise an exception. Verify that `AsyncQdrantClient.delete` is called (via `delete_document_points`) and the exception is re-raised.
  </action>
  <verify>
    <automated>pytest tests/test_indexing_pipeline.py</automated>
  </verify>
  <done>Resiliency and consistency tests verify retry and rollback behaviors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Pipeline | Test code exercises the pipeline logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | R | Test assertions | mitigate | Use strict mocks and verified fakes to avoid false positives. |
</threat_model>

<verification>
Run all tests in `tests/test_indexing_pipeline.py` and ensure they pass with `pytest-asyncio`.
</verification>

<success_criteria>
`index_document` passes happy-path, retry-success, retry-failure, and rollback tests.
</success_criteria>

<output>
After completion, create `.planning/phases/18-indexing-pipeline-mvp/18-03-SUMMARY.md`
</output>
