---
phase: 18-indexing-pipeline-mvp
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified: [corpulse/pipelines/indexing.py]
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03]
must_haves:
  truths:
    - "index_document orchestrates parse, chunk, embed, upsert, and register"
    - "Embedder calls include exponential backoff retries"
    - "Failed register_document calls trigger Qdrant point deletion"
  artifacts:
    - path: "corpulse/pipelines/indexing.py"
      provides: "Resilient indexing pipeline implementation"
  key_links:
    - from: "index_document"
      to: "corpulse.integrations.qdrant.delete_document_points"
      via: "rollback try-except block"
---

<objective>
Implement orchestration, retries, and rollback behavior in `index_document`.

Purpose: Ensure the indexing process is resilient and maintains consistency between the vector database and the Corpulse registry.
Output: A fully functional `index_document` implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-indexing-pipeline-mvp/18-RESEARCH.md
@corpulse/pipelines/indexing.py
@corpulse/integrations/qdrant.py
@corpulse/async_core.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Pipeline Orchestration</name>
  <files>corpulse/pipelines/indexing.py</files>
  <action>
Implement the main orchestration flow in `index_document`:
1. Use `time.perf_counter` to measure duration.
2. Call `parser.parse(source)`.
3. Call `chunker.chunk(text)`.
4. Call `embedder.embed(chunks)`.
5. Prepare Qdrant points (`PointStruct`):
   - Use `corpulse.integrations.qdrant.chunk_id(doc_id, index)` for deterministic UUIDs.
   - Every point MUST include `payload={"doc_id": doc_id}` (Pitfall 1).
6. Call `client.upsert(collection_name=collection_name, points=points)`.
7. Calculate the mean embedding from the chunk embeddings using `numpy.mean(embeddings, axis=0)`.
8. Call `corpulse.register_document(doc_id, filename, mean_embedding)`.
9. Return `IndexingResult`.

Handle necessary imports from `qdrant_client.models`, `numpy`, `time`, and `corpulse.integrations.qdrant`.
  </action>
  <verify>
    <automated>python3 -c "import corpulse.pipelines.indexing as idx; import inspect; print(inspect.getsource(idx.index_document))" | grep "register_document"</automated>
  </verify>
  <done>Full orchestration flow is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Embedding Retries</name>
  <files>corpulse/pipelines/indexing.py</files>
  <action>
Wrap the `embedder.embed(chunks)` call with retry logic:
- Max attempts: 3.
- Backoff: Exponential (2, 4, 8 seconds).
- Catch: Generic `Exception` from the embedder call.
- Behavior: Log a warning on failure and retry. Raise the error after final attempt.
  </action>
  <verify>
    <automated>python3 -c "import corpulse.pipelines.indexing as idx; import inspect; print(inspect.getsource(idx.index_document))" | grep "asyncio.sleep"</automated>
  </verify>
  <done>Retry logic is implemented and covers the embedding step.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Qdrant Rollback</name>
  <files>corpulse/pipelines/indexing.py</files>
  <action>
Add a `try...except` block around the `corpulse.register_document` call:
- If `register_document` fails, catch the exception.
- Call `corpulse.integrations.qdrant.delete_document_points(client, collection_name, doc_id)` to remove the vectors from Qdrant.
- Ensure the deletion is awaited if the client is async (it will be).
- Re-raise the original exception.
This ensures "ghost" points are not left in Qdrant if Corpulse registry fails (PIPE-03).
  </action>
  <verify>
    <automated>python3 -c "import corpulse.pipelines.indexing as idx; import inspect; print(inspect.getsource(idx.index_document))" | grep "delete_document_points"</automated>
  </verify>
  <done>Rollback logic is implemented and ensures consistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → Qdrant | Pipeline sends vectors and metadata to Qdrant (untrusted input to vector DB). |
| Pipeline → Corpulse | Pipeline registers metadata in Corpulse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | I | `delete_document_points` | mitigate | Rollback logic ensures that failed registrations do not leave stale data. |
| T-18-04 | D | `AsyncQdrantClient` | mitigate | Use standard Qdrant client methods which handle transient network issues (plus pipeline-level retries for embeddings). |
</threat_model>

<verification>
Review the code for:
- Correct use of `chunk_id` for deterministic IDs.
- Correct use of `delete_document_points` for rollback.
- Correct mean embedding calculation.
- Resilient retry loop.
</verification>

<success_criteria>
`index_document` correctly orchestrates the pipeline, handles retries for embeddings, and performs rollback on Qdrant if Corpulse registration fails.
</success_criteria>

<output>
After completion, create `.planning/phases/18-indexing-pipeline-mvp/18-02-SUMMARY.md`
</output>
