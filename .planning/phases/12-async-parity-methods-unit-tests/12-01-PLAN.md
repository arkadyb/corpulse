---
phase: 12-async-parity-methods-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/report_fixtures.py
  - tests/test_report_helpers.py
  - tests/test_async_core_integration.py
  - corpulse/async_core.py
autonomous: true
requirements: [ASYNC-PAR-01, ASYNC-TEST-01]

must_haves:
  truths:
    - "AsyncCorpulse.to_dataframe(window_days) returns the same columns, ordering, and status classifications as sync Corpulse on the same frozen fixture."
    - "The async dataframe path keeps pandas optional and raises the same install-hint RuntimeError as the sync path."
    - "Sync and async parity tests draw from one authoritative frozen report fixture rather than duplicated setup."
  artifacts:
    - path: "tests/report_fixtures.py"
      provides: "Shared deterministic report-fixture builders usable by sync and async tests"
      min_lines: 60
    - path: "corpulse/async_core.py"
      provides: "AsyncCorpulse.to_dataframe implementation reusing shared helper logic and sync pandas guard semantics"
      contains: "async def to_dataframe"
    - path: "tests/test_async_core_integration.py"
      provides: "Async dataframe parity and pandas-guard tests against the shared fixture"
      contains: "to_dataframe"
    - path: "tests/test_report_helpers.py"
      provides: "Sync helper tests rewritten to import the shared frozen fixture source"
      contains: "report_fixtures"
  key_links:
    - from: "tests/report_fixtures.py"
      to: "tests/test_report_helpers.py"
      via: "shared sync fixture imports"
      pattern: "from tests\\.report_fixtures import"
    - from: "tests/report_fixtures.py"
      to: "tests/test_async_core_integration.py"
      via: "shared async parity fixture imports"
      pattern: "from tests\\.report_fixtures import"
    - from: "corpulse/async_core.py"
      to: "corpulse/core.py"
      via: "shared dataframe row helper and pandas guard reuse"
      pattern: "_build_dataframe_rows|RuntimeError\\(\"pip install pandas to use to_dataframe\\(\\)\"\\)"
---

<objective>
Create the shared deterministic fixture seam for report parity work, then add `AsyncCorpulse.to_dataframe()` with exact sync parity and the same optional-pandas failure behavior.

Purpose: Phase 12 must prove same-fixture parity, not merely similar behavior. Establishing one frozen fixture source first removes drift risk and gives the async dataframe method a reliable parity gate.
Output: A reusable report-fixture module, sync tests rewired to that module, and async dataframe implementation/tests that compare directly against sync output on the same backend data.
</objective>

<execution_context>
@/Users/arkady/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arkady/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-async-parity-methods-unit-tests/12-RESEARCH.md
@.planning/phases/12-async-parity-methods-unit-tests/12-VALIDATION.md
@.planning/phases/10-async-backend-corpulse-integration/10-01-SUMMARY.md
@.planning/phases/11-shared-report-helpers/11-01-SUMMARY.md
@.planning/phases/11-shared-report-helpers/11-02-SUMMARY.md
@.planning/phases/11-shared-report-helpers/11-03-SUMMARY.md
@corpulse/core.py
@corpulse/async_core.py
@tests/test_report_helpers.py
@tests/test_async_core_integration.py
@tests/conftest.py

<interfaces>
From corpulse/core.py:
```python
def _build_dataframe_rows(
    all_docs: list[dict[str, Any]],
    r_map: dict[str, dict[str, Any]],
    e_map: dict[str, int],
    ghost_ids: set[str],
    obsolete_ids: set[str],
    stale_ids: set[str],
) -> list[dict[str, Any]]: ...
```

```python
class Corpulse:
    def to_dataframe(self, window_days: int | None = None):
        try:
            import pandas as pd
        except ImportError:
            raise RuntimeError("pip install pandas to use to_dataframe()")
```

From corpulse/async_core.py:
```python
class AsyncCorpulse:
    async def get_ghosts(self) -> list[dict]: ...
    async def get_obsolete(self) -> list[dict]: ...
    async def get_stale_embeddings(self) -> list[dict]: ...
    async def get_suspects(self, window_days: int | None = None) -> list[dict]: ...
```

From tests/test_report_helpers.py:
```python
FROZEN = 1_700_000_000.0
def _report_fixture_backend() -> InMemoryBackend: ...
def _helper_inputs(): ...
```

From tests/test_async_core_integration.py:
```python
class FakeAsyncBackend:
    async def all_documents(self) -> list[dict]: ...
    async def retrieval_counts(self, since: float) -> list[dict]: ...
    async def engagement_counts(self, since: float) -> list[dict]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract one authoritative frozen report fixture source for sync and async parity per planner discretion</name>
  <files>tests/report_fixtures.py, tests/test_report_helpers.py, tests/test_async_core_integration.py</files>
  <behavior>
    - Test 1: Sync report-helper tests still reproduce the exact Phase 11 frozen data and golden stdout after importing the fixture from one shared module.
    - Test 2: Async parity tests can construct both a sync backend and an async fake backend from the same seeded report fixture rows.
  </behavior>
  <action>Create `tests/report_fixtures.py` as the single source of truth for the deterministic report corpus described in Phase 11, using the same frozen timestamp, document set, retrieval counts, engagement counts, and embedding seeds already proven by the golden stdout tests. Move only the fixture-building logic, not the assertions or golden strings. Rewire `tests/test_report_helpers.py` to import from the new module so Phase 11 regression coverage remains intact. Add helper functions for the async suite that expose raw seeded rows and a sync `InMemoryBackend` builder, so later tasks can build a fake async backend from the exact same data. Do not weaken or recapture any Phase 11 golden assertions; preserve them byte-for-byte per the Phase 11 decisions.</action>
  <verify>
    <automated>pytest tests/test_report_helpers.py -q</automated>
  </verify>
  <done>`tests/report_fixtures.py` exists, `tests/test_report_helpers.py` imports from it without changing the golden constants, and the sync helper suite still passes against the shared fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add and implement async dataframe parity per ASYNC-PAR-01 and ASYNC-TEST-01</name>
  <files>tests/test_async_core_integration.py, corpulse/async_core.py</files>
  <behavior>
    - Test 1: `await AsyncCorpulse.to_dataframe(window_days=30)` returns the same columns, row order, row values, and statuses as `Corpulse.to_dataframe(window_days=30)` on the shared frozen fixture.
    - Test 2: The async dataframe method sorts rows by retrievals descending exactly as the sync surface does.
    - Test 3: If pandas import fails, `AsyncCorpulse.to_dataframe()` raises `RuntimeError("pip install pandas to use to_dataframe()")`.
  </behavior>
  <action>In `tests/test_async_core_integration.py`, add parity tests that build a sync `Corpulse` and an async `AsyncCorpulse` from the same report fixture source created in Task 1. Normalize both DataFrames with `to_dict("records")` so parity is asserted on data, not object identity. Cover the pandas-missing branch with the same monkeypatch import pattern already used by `tests/test_report_helpers.py`. Then implement `AsyncCorpulse.to_dataframe()` in `corpulse/async_core.py` as a thin awaited analogue of sync `Corpulse.to_dataframe()`: fetch documents and counts with `await`, derive `ghost_ids`, `obsolete_ids`, and `stale_ids` through the existing async analysis methods, pass those inputs into `_build_dataframe_rows(...)`, and return a pandas DataFrame sorted by `retrievals` descending. Reuse the exact sync install-hint string and do not introduce new async-specific payload logic.</action>
  <verify>
    <automated>pytest tests/test_async_core_integration.py -q</automated>
  </verify>
  <done>`AsyncCorpulse.to_dataframe()` exists, matches the sync DataFrame contract on the shared frozen fixture, preserves the exact pandas guard string, and the focused async suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async facade to backend rows | `AsyncCorpulse` consumes backend-provided document and aggregate rows that must stay aligned with sync helper expectations |
| test fixture to parity assertions | The parity suite trusts one shared frozen fixture source; duplicate fixture setup would let silent drift evade detection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | `tests/report_fixtures.py` and parity tests | mitigate | Use one authoritative frozen fixture module and import it from both sync and async suites so same-fixture parity is enforced directly rather than duplicated manually. |
| T-12-02 | D | `corpulse/async_core.py::to_dataframe` | mitigate | Preserve the exact pandas `RuntimeError` guard string from sync `Corpulse.to_dataframe()` and cover it with an explicit async test so missing optional dependencies fail clearly instead of crashing unpredictably. |
| T-12-03 | T | `corpulse/async_core.py::to_dataframe` | mitigate | Reuse `_build_dataframe_rows(...)` from `corpulse.core` instead of reimplementing status logic in the async path, preventing async-only classification drift from the sync contract. |
</threat_model>

<verification>
Run `pytest tests/test_report_helpers.py -q` after fixture extraction, then `pytest tests/test_async_core_integration.py -q` after landing `AsyncCorpulse.to_dataframe()`. The phase wave check for this plan is `pytest tests/test_async_core_integration.py tests/test_report_helpers.py -q`.
</verification>

<success_criteria>
`tests/report_fixtures.py` is the only frozen report fixture source, sync golden tests remain green unchanged, `AsyncCorpulse.to_dataframe()` exists, and deterministic async parity tests prove it matches the sync dataframe surface including the pandas-missing failure message.
</success_criteria>

<output>
After completion, create `.planning/phases/12-async-parity-methods-unit-tests/12-01-SUMMARY.md`
</output>
