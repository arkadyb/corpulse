---
phase: 16-postgres-multi-tenancy
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - corpulse/backends/postgres.py
  - tests/test_postgres_backend.py
autonomous: true
requirements:
  - PGMT-03
  - PGMT-04
tags: [postgres, multitenancy, ddl]
---

<objective>
Introduce the shared tenancy primitives for Postgres backends: strict identifier validation plus a public `build_schema_sql(schema=None, prefix="")` helper that generates backward-compatible table/index DDL and optional schema bootstrap SQL.

Output:
- shared identifier validation in `corpulse/backends/postgres.py`
- public `build_schema_sql(schema=None, prefix="")`
- direct tests for DDL generation and invalid identifier rejection
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-postgres-multi-tenancy/16-RESEARCH.md
@.planning/phases/16-postgres-multi-tenancy/16-VALIDATION.md
@corpulse/backends/postgres.py
@tests/test_postgres_backend.py
</context>

<tasks>

<task>
  <name>Task 1: Add identifier validation helpers</name>
  <files>corpulse/backends/postgres.py</files>
  <action>
    Add a private regex-backed validator for `schema` and `table_prefix`. Permit `schema=None` and `table_prefix=""`; reject any other non-matching value with `ValueError` before SQL is generated.
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py -k identifier -x</automated>
  </verify>
</task>

<task>
  <name>Task 2: Add public `build_schema_sql(schema=None, prefix="")`</name>
  <files>corpulse/backends/postgres.py</files>
  <action>
    Replace the static schema constant with a function builder that emits:
    - optional `CREATE SCHEMA IF NOT EXISTS ...`
    - tenant-aware table names for `documents`, `retrievals`, and `engagements`
    - tenant-aware index names to avoid collisions in prefix-only mode
    Preserve current SQL shape when called with defaults.
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py -k schema_sql -x</automated>
  </verify>
</task>

<task>
  <name>Task 3: Lock down helper behavior with direct tests</name>
  <files>tests/test_postgres_backend.py</files>
  <action>
    Add tests that call `build_schema_sql()` directly for:
    - backward-compatible default output
    - schema-qualified output
    - prefix-only output
    - invalid identifier rejection
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py -k "schema_sql or identifier" -x</automated>
  </verify>
</task>

</tasks>
