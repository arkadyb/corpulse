---
phase: 16-postgres-multi-tenancy
plan: 03
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - tests/test_postgres_backend.py
  - tests/test_async_postgres_backend.py
autonomous: true
requirements:
  - PGMT-05
  - PGMT-04
tags: [postgres, multitenancy, tests]
---

<objective>
Prove the tenancy refactor is safe by adding regression coverage for invalid identifiers, prefix-only mode, and schema isolation behavior across sync and async Postgres backends.

Output:
- regression tests that assert prefixed and schema-qualified SQL usage
- per-schema isolation coverage on one database where the env-gated live tests allow it
- phase-level confidence that tenants do not cross-contaminate
</objective>

<context>
@.planning/phases/16-postgres-multi-tenancy/16-RESEARCH.md
@.planning/phases/16-postgres-multi-tenancy/16-VALIDATION.md
@tests/test_postgres_backend.py
@tests/test_async_postgres_backend.py
</context>

<tasks>

<task>
  <name>Task 1: Add prefix-only and invalid-identifier regressions</name>
  <files>tests/test_postgres_backend.py, tests/test_async_postgres_backend.py</files>
  <action>
    Add tests that prove prefix-only mode rewrites table names correctly and invalid identifiers raise before SQL executes on both backends.
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py tests/test_async_postgres_backend.py -x</automated>
  </verify>
</task>

<task>
  <name>Task 2: Add per-schema isolation coverage</name>
  <files>tests/test_postgres_backend.py, tests/test_async_postgres_backend.py</files>
  <action>
    Use the strongest available test shape to prove two instances on one database with different schemas do not cross-read each other. Prefer live env-gated coverage if the existing Postgres test setup supports it; otherwise capture schema-qualified SQL paths explicitly and keep the live case conditional.
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py tests/test_async_postgres_backend.py -x</automated>
  </verify>
</task>

</tasks>
