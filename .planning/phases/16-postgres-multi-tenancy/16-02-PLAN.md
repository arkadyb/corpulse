---
phase: 16-postgres-multi-tenancy
plan: 02
type: execute
wave: 1
depends_on:
  - 16-01
files_modified:
  - corpulse/backends/postgres.py
  - corpulse/backends/postgres_async.py
  - tests/test_postgres_backend.py
  - tests/test_async_postgres_backend.py
autonomous: true
requirements:
  - PGMT-01
  - PGMT-02
tags: [postgres, multitenancy, async]
---

<objective>
Refactor both Postgres backends to accept validated tenancy configuration and use qualified table helpers for every DDL/DML path without changing default behavior.

Output:
- `PostgresBackend(..., schema=None, table_prefix="")`
- `AsyncPostgresBackend.create(..., schema=None, table_prefix="")`
- no remaining hardcoded table names in query paths
</objective>

<context>
@.planning/phases/16-postgres-multi-tenancy/16-RESEARCH.md
@corpulse/backends/postgres.py
@corpulse/backends/postgres_async.py
@tests/test_postgres_backend.py
@tests/test_async_postgres_backend.py
</context>

<tasks>

<task>
  <name>Task 1: Rewire sync backend to use qualified table helpers</name>
  <files>corpulse/backends/postgres.py, tests/test_postgres_backend.py</files>
  <action>
    Extend the sync constructor with `schema` and `table_prefix`, validate/store both, replace static schema execution with `build_schema_sql(...)`, and route all SQL through `self._t("documents")`-style helpers.
  </action>
  <verify>
    <automated>pytest tests/test_postgres_backend.py -x</automated>
  </verify>
</task>

<task>
  <name>Task 2: Rewire async backend to consume shared DDL and table helpers</name>
  <files>corpulse/backends/postgres_async.py, tests/test_async_postgres_backend.py</files>
  <action>
    Extend `AsyncPostgresBackend.create` with `schema` and `table_prefix`, validate/store both, initialize through `build_schema_sql(...)`, and replace hardcoded query table names with an async instance helper mirroring sync behavior.
  </action>
  <verify>
    <automated>pytest tests/test_async_postgres_backend.py -x</automated>
  </verify>
</task>

</tasks>
