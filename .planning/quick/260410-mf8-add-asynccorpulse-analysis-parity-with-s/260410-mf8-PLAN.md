---
type: quick-plan
mode: quick
autonomous: true
files_modified:
  - corpulse/core.py
  - corpulse/async_core.py
  - tests/test_async_core_integration.py
must_haves:
  truths:
    - "AsyncCorpulse exposes the same analysis methods as sync Corpulse for duplicates, obsolete docs, stale embeddings, suspects, and corpus health."
    - "Each async analysis method returns the same payload shape and threshold behavior as the sync implementation for equivalent backend data."
    - "Sync Corpulse behavior remains unchanged while AsyncCorpulse reuses shared analysis logic where practical."
    - "The async integration test suite proves the new analysis methods await backend reads correctly and match sync semantics."
  artifacts:
    - path: "corpulse/async_core.py"
      provides: "Async analysis surface for get_duplicates, get_obsolete, get_stale_embeddings, get_suspects, and corpus_health"
    - path: "corpulse/core.py"
      provides: "Reusable analysis helpers or shared logic extraction that keeps sync outputs stable while enabling async parity"
    - path: "tests/test_async_core_integration.py"
      provides: "Async parity coverage for the new analysis methods using fake and live async backends"
  key_links:
    - from: "corpulse/async_core.py"
      to: "corpulse/core.py"
      via: "Shared helper functions for pure analysis logic after async backend reads"
    - from: "corpulse/async_core.py"
      to: "tests/test_async_core_integration.py"
      via: "New public async methods exercised through fake backend and integration-style assertions"
---

<objective>
Add AsyncCorpulse analysis parity with sync Corpulse for the missing analysis methods: `get_duplicates`, `get_obsolete`, `get_stale_embeddings`, `get_suspects`, and `corpus_health`.

Purpose: Make the async API usable for the same corpus-analysis flows as the sync API without changing sync behavior or spilling into service-repo work.
Output: Async analysis methods implemented, any needed shared helper extraction in `corpulse/core.py`, and async integration tests covering parity-critical cases.
</objective>

<context>
@.planning/STATE.md
@corpulse/core.py
@corpulse/async_core.py
@corpulse/backends/postgres_async.py
@tests/test_async_core_integration.py

<constraints>
- Keep the scope to AsyncCorpulse analysis parity only.
- Do not change sync Corpulse behavior or public payload shapes.
- Do not add service-layer work, roadmap edits, or unrelated backend changes.
- Prefer extracting or reusing pure sync helper logic instead of duplicating analysis rules in async code.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract reusable analysis helpers from sync Corpulse without changing sync outputs</name>
  <files>corpulse/core.py</files>
  <behavior>
    - Sync duplicate, obsolete, stale-embedding, suspect, and corpus-health calculations continue returning the same shapes and thresholds as today.
    - The reusable pieces are pure helper logic that can operate on already-fetched rows, so AsyncCorpulse only needs to await backend reads and then call the same logic.
    - Duplicate detection still preserves the existing scikit-learn guard and duplicate-count behavior in corpus health.
  </behavior>
  <action>Refactor `corpulse/core.py` to isolate the analysis computations that do not need direct sync backend access. Keep public sync methods and their behavior unchanged, but move row-processing logic into private helpers that accept document, embedding, retrieval, and engagement rows as inputs. Preserve current defaults, recommendation text, sorting, and `_SKLEARN` handling. Do not introduce a new public API solely for the refactor.</action>
  <verify>
    <automated>pytest tests/test_async_core_integration.py -q</automated>
  </verify>
  <done>`corpulse/core.py` contains reusable private helpers for the missing analysis methods, and the sync class still delegates through its existing public methods with unchanged behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the missing AsyncCorpulse analysis methods on top of shared logic</name>
  <files>corpulse/async_core.py, corpulse/core.py</files>
  <behavior>
    - `await AsyncCorpulse.get_duplicates()` matches sync payload shape and threshold semantics, including the scikit-learn runtime error path when unavailable.
    - `await AsyncCorpulse.get_obsolete()`, `await AsyncCorpulse.get_stale_embeddings()`, and `await AsyncCorpulse.get_suspects()` match sync output structure and filtering rules for the same backend rows.
    - `await AsyncCorpulse.corpus_health()` computes the same counts, noise estimate, warning flag, and recommendation text as the sync method for equivalent data.
  </behavior>
  <action>Add the five missing async analysis methods to `AsyncCorpulse`. Each method should await only the backend reads it needs, then reuse the extracted helper logic from `corpulse/core.py` rather than re-implementing the rules. Keep `get_ghosts()` behavior as-is. For `corpus_health()`, preserve the sync method's empty-corpus behavior, duplicate counting semantics, and recommendation wording. Avoid changing backend interfaces; consume the existing async read methods already present on `AsyncPostgresBackend`.</action>
  <verify>
    <automated>pytest tests/test_async_core_integration.py -q</automated>
  </verify>
  <done>`AsyncCorpulse` exposes all five missing analysis methods and they follow sync semantics while using async backend reads correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend async integration coverage for parity and await behavior</name>
  <files>tests/test_async_core_integration.py</files>
  <behavior>
    - The fake async backend covers `all_embeddings()` and `engagement_counts()` so the new analysis methods can be exercised deterministically.
    - Tests verify representative parity cases for obsolete detection, stale embeddings, suspects, and corpus health, plus duplicate detection with controlled embeddings.
    - Tests assert backend call patterns where practical so the async methods prove they await backend reads instead of relying on sync-only assumptions.
  </behavior>
  <action>Expand `tests/test_async_core_integration.py` with focused async tests for the five new methods. Update `FakeAsyncBackend` to serve documents, embeddings, retrieval counts, and engagement counts needed by the analysis paths. Cover at least one end-to-end health case that combines ghost/obsolete/stale/duplicate signals and one duplicate case that validates similarity output shape and ordering. Use monkeypatching for time-sensitive thresholds and `_SKLEARN`-sensitive branches as needed so the test suite stays deterministic.</action>
  <verify>
    <automated>pytest tests/test_async_core_integration.py -q</automated>
  </verify>
  <done>The async integration test file proves AsyncCorpulse analysis parity for the missing methods and guards against regressions in payload shape or backend-await behavior.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_async_core_integration.py -q` and confirm the async suite covers and passes for the new analysis methods without requiring any sync API behavior changes.
</verification>

<success_criteria>
- `AsyncCorpulse` implements `get_duplicates`, `get_obsolete`, `get_stale_embeddings`, `get_suspects`, and `corpus_health`.
- Shared analysis logic is reused from `corpulse/core.py` where practical instead of duplicated in async code.
- Async test coverage proves parity for payload shape, filtering thresholds, duplicate counting, and health-summary output.
- No service-repo work, roadmap files, or unrelated sync behavior changes are introduced.
</success_criteria>
